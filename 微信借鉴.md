# 微信 PC 群聊 — LanPM 可借鉴设计备忘

> **用途**：记录微信桌面版群聊界面中**值得借鉴的交互与信息结构**，并明确 **LanPM 不必照搬** 的部分。  
> **背景**：LanPM 是项目协作桌面客户端（Apple HIG + Ant Design），不是纯 IM。  
> **更新**：2026-05-29

---

## 1. 产品差异（为何不能照抄微信）

| 维度 | 微信 | LanPM |
|------|------|-------|
| **群 / 会话数量** | 用户常有几十～上百个聊天 | 每人参与的项目群**数量有限**（通常个位数～十数个） |
| **群的语义** | 社交群、工作群、公众号等混合 | **一个群 = 一个项目**（或职能群 / 匿名群） |
| **核心任务** | 以聊天为主 | 聊天 + 看板 + 任务树 + 甘特 + 文件 |
| **导航重心** | 左侧会话列表是主入口 | **TopBar 切项目** + **BottomNav 切视图** |
| **侧栏角色** | 无独立「成员列表」栏 | 聊天页左侧固定 **成员列表**（协作场景更重要） |

**结论**：微信的 **三栏 IM 骨架**（图标栏 + 会话列表 + 聊天区）对 LanPM **优先级低**；应优先借鉴 **单会话内的聊天体验** 和 **少量通用 IM 习惯**，而不是复制「多会话管理中心」。

---

## 2. 微信三栏结构对照（仅作理解，非目标架构）

```
微信 PC 典型布局
┌──────┬──────────────┬────────────────────────────┐
│ 图标 │  会话列表     │  当前聊天（群名/消息/输入）   │
│ 栏   │  搜索+预览+时间│                            │
└──────┴──────────────┴────────────────────────────┘

LanPM 现有布局（见 docs/01 §6.1、docs/04 §6.1）
┌─ TopBar：项目下拉 / 搜索 / 设置 ─────────────────┐
├─ 聊天视图 ────────────────────────────────────────┤
│ ┌ 成员列表 240px ┐ ┌ 消息区 + Composer ─────────┐ │
│ │ 在线状态 / @   │ │ 群聊|私聊 Segmented         │ │
│ └────────────────┘ └────────────────────────────┘ │
└─ BottomNav：聊天 / 看板 / 树 / 甘特 / 文件 ───────┘
```

| 微信区域 | LanPM 对应 | 是否借鉴 |
|----------|------------|----------|
| 最左图标栏 | `BottomNav` + `TopBar` | ❌ 不复制；功能已分散到协作 Tab |
| 中间会话列表 | `TopBar` 项目下拉；私聊见 `DmSessionBar` | ⚠️ **弱借鉴**（见 §4.2） |
| 右侧聊天主区 | `ChatView` 消息 + Composer | ✅ **主要借鉴区** |
| （无）成员侧栏 | `MemberList` | ✅ LanPM 独有优势，保留 |

---

## 3. 建议借鉴（按优先级）

### 3.1 P0 — 单会话聊天体验（高价值、低架构成本）

这些模式**不依赖**「很多群 / 很多会话」，与「一群一项目」完全兼容。

| 模式 | 微信做法 | LanPM 建议 | 状态 |
|------|----------|------------|------|
| **气泡布局** | 他人左对齐 + 头像 + 昵称；自己右对齐 | 他人 `--lanpm-bubble-bg`，自己 `--lanpm-accent` | ✅ 已实现 `MessageBubble.tsx` |
| **连续消息压缩** | 同一人短时间多条，省略重复头像/昵称 | `showSender` 控制 | ✅ 已实现 |
| **日期分隔** | 居中灰色时间 / 日期 | 居中 pill `dayLabel` | ✅ 已实现 |
| **输入工具栏** | 表情、文件、截图等图标在输入框上方 | Composer `toolbar` | ✅ 已实现 |
| **Enter 发送** | Enter 发送，Shift+Enter 换行 | 同左 + 输入区提示文案 | ✅ 已实现（UX-F-08） |
| **拖拽调输入区高度** | 输入区可拉高 | Composer 可拖拽高度 | ✅ 已实现 |
| **文件拖入发送** | 拖文件到聊天窗口 | `fileDropOverlay` | ✅ 已实现 |
| **系统 / 撤回提示** | 居中弱提示 | 若有撤回/系统消息类型，居中弱样式 | ✅ 系统消息已实现（WX-04）；撤回待协议 |
| **新消息跳转** | 「N 条新消息」浮动按钮，点击滚到底 | 用户上滑看历史时显示 | ✅ 已实现（WX-01） |

### 3.2 P1 — 全局感知（群少时仍有用）

群数量不多，**未读 / 待办角标** 比「长会话列表」更重要。

| 模式 | 微信做法 | LanPM 建议 | 状态 |
|------|----------|------------|------|
| **Tab 未读角标** | 会话列表未读红点 / 数字 | `BottomNav` 聊天 Tab 显示当前群未读 | ✅ 已实现（UX-F-18） |
| **看板待办角标** | （无直接对应） | 看板 Tab 显示待办根任务数 | ✅ 已实现 |
| **桌面通知** | 新消息 / @ 提醒 | 新消息、@提及 通知 | ✅ @ + 可选全量（WX-06） |
| **已读回执** | 单勾 / 双勾 | 投递状态 + 同 userId 聚合已读 | ✅ 已实现 |

### 3.3 P2 — 私聊与会话（弱借鉴）

LanPM 私聊会话数通常 **≪ 微信群数**，不必做微信级「中间栏会话中心」。

| 模式 | 微信做法 | LanPM 建议 | 状态 |
|------|----------|------------|------|
| **私聊入口** | 会话列表里每个联系人一行 | 主区「群聊 \| 私聊」+ `DmSessionBar` | ✅ 已实现 |
| **会话行信息** | 头像 + 名 + 最后一条 + 时间 | DM 行可补 **最后消息预览 / 时间**（可选） | ✅ 已实现（WX-02） |
| **选中高亮** | 列表项灰底 | `dmSessionItemActive` 强调色底 | ✅ 已实现 |
| **多群会话列表** | 中间栏滚动几十条 | **不需要**；TopBar 下拉足够 | ❌ 不做 |

### 3.4 P3 — 聊天区内嵌顶栏（可选）

微信右侧顶栏：**群名 (人数) + 置顶 + ⋯ 菜单**。

LanPM 群名已在 `TopBar` / `ViewHeader`，若聊天区信息密度不足，可考虑轻量 **in-chat header**（仅群名、人数、快捷进成员/文件），**不必**复制微信置顶/免打扰全套。

| 项 | 建议 |
|----|------|
| 群名 + 成员数 | 可选，避免与 TopBar 重复 |
| 快捷「看成员 / 群文件」 | 与协作 Tab 联动时可考虑 |
| 置顶 / 免打扰 | 项目群场景优先级低 |

---

## 4. 明确不借鉴

| 微信特征 | 不借鉴原因 |
|----------|------------|
| **三栏 IM 骨架** | LanPM 已是 TopBar + BottomNav + 两栏聊天；再加会话列冗余 |
| **中间栏「海量会话列表」** | 项目群数量少；切换成本用 TopBar 下拉即可 |
| **微信绿 `#07C160` 主色** | 破坏 `--lanpm-accent` 设计体系（蓝系 Apple 风） |
| **最左全局图标栏** | 与 BottomNav 五视图重复 |
| **语音/视频通话主入口** | 非 P0 协作能力 |
| **朋友圈 / 视频号 / 公众号** | 与产品无关 |
| **纯社交型群设置** | 群公告、群二维码等；LanPM 走项目 / 局域网发现模型 |

---

## 5. 待办增强（可选 backlog）

与「群 = 项目、数量不多」一致、且仍值得从微信学习的项：

| ID | 项 | 说明 | 状态 |
|----|-----|------|------|
| WX-01 | **新消息跳转按钮** | 监听 scroll + `listRef` | ✅ |
| WX-02 | **DM 最后消息预览** | `DmSessionBar` + `chatStore` | ✅ |
| WX-03 | **聊天区 in-chat header** | 在线人数 + 群文件快捷入口 | ✅ |
| WX-04 | **撤回 / 系统消息样式** | `system` kind 居中弱文案 | ✅ 系统；撤回待协议 |
| WX-05 | **搜索命中 scroll + 高亮** | `GlobalSearch` → `location.state` | ✅（此前已做） |
| WX-06 | **桌面通知（全量新消息）** | 个人设置开关 + `useChatNotifications` | ✅ |

> 已关闭项（勿重复规划）：Enter 发送、日期分组、BottomNav 角标、发送者 displayName 映射 — 见 CHANGELOG / `archive/todo/`。

---

## 6. 视觉与令牌（借交互，不借皮）

借鉴微信的**结构**，沿用 LanPM 的**令牌**（`docs/04` §1.5）：

| 元素 | LanPM 令牌 / 约定 |
|------|-------------------|
| 他人气泡 | `--lanpm-bubble-bg`，圆角 **8px** |
| 自己气泡 | `--lanpm-accent` + `--lanpm-on-accent` |
| 分隔线 | `--lanpm-border` |
| 选中 / hover | `--lanpm-hover-bg`、`--lanpm-accent-fill-strong` |
| 时间 / 辅助文案 | `--lanpm-font-caption`、`--lanpm-text-secondary` |
| 主强调色 | **禁止**硬编码微信绿或 `#1677ff` |

---

## 7. 代码与文档索引

| 主题 | 位置 |
|------|------|
| 聊天布局与侧栏 | `src/renderer/src/features/chat/ChatView.tsx` |
| 气泡与发送者 | `src/renderer/src/features/chat/MessageBubble.tsx` |
| 样式 | `src/renderer/src/features/chat/chat.module.css` |
| 私聊会话 | `src/renderer/src/features/chat/DmSessionBar.tsx` |
| 成员列表 | `src/renderer/src/features/chat/MemberList.tsx` |
| 未读角标 | `src/renderer/src/layout/BottomNav.tsx`、`badgeStore` |
| 产品聊天需求 | `docs/01_产品需求文档.md` §6 |
| UI 约定 | `docs/04_交互与UI约定.md` §6.1 |
| 与微信习惯差异（历史） | `archive/review/20260529_095839_UI设计审查_ui.md` F-08、F-18 |

---

## 8. 设计原则（一句话）

**从微信借「聊天手感」和「IM 微习惯」，不借「多会话管理中心」和「微信视觉」；LanPM 的群是项目，导航重心在 TopBar + 协作五视图，成员侧栏才是差异化。**
