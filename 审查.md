# LanPM 全仓审查报告

**审查日期：** 2026-07-30  
**工作目录：** `/home/jwzhou/workspace/lanpm`  
**审查范围：** 国际化（i18n）· 视觉一致性 · 文字对比度 · 死代码  
**审查方式：** 静态代码扫描 + 设计令牌分析 + 子模块并行审计 + 关键项人工复核  
**当前分支：** `master`（与 `origin/master` 一致）

---

## 执行摘要

| 维度 | 总体评价 | Blocker | High | Medium | Low |
|------|----------|---------|------|--------|-----|
| 国际化（i18n） | ⭐⭐⭐⭐ 良好，键完整对称 | 0 | 1 | 4 | 5 |
| 视觉一致性 | ⭐⭐⭐⭐ 令牌体系成熟，局部偏差 | 0 | 3 | 8 | 6 |
| 文字对比度 | ⭐⭐⭐ 亮色 tertiary 系统性不足 | 0 | 3 | 9 | 5 |
| 死代码 | ⭐⭐⭐⭐ 整体干净，少量遗留 | 0 | 2 | 4 | 2 |

**结论：** 核心功能视图均已接入 i18n，zh-CN / en-US 987 键 1:1 对称；设计令牌 SSOT 与 `verify:visual` 守卫较完善。主要缺口集中在：**AI 提供商预设中文泄漏**、**亮色三级文字色 WCAG 不达标**、**2 个零引用组件文件**、**首屏主题与 uiStore 逻辑分叉**、**白板暗色仍白底**。

---

## 1. 国际化（i18n）审查

### 1.1 架构概览

项目采用**自研轻量 i18n**（非 react-i18next）：

```
src/renderer/src/i18n/
├── locales/zh-CN.ts      # 987 条（默认语言）
├── locales/en-US.ts      # 987 条
├── types.ts              # MessageKey 联合类型
├── messages.ts           # MESSAGES + translate()
├── useI18n.ts            # React Hook（locale / t / formatError）
├── navKeys.ts            # 导航视图 → MessageKey
├── groupLabels.ts        # 群组展示名
├── memberDisplay.ts      # 成员展示名
├── presence.ts           # 在线状态
└── formatAppError.ts     # 错误码本地化
```

| 环节 | 实现 |
|------|------|
| 语言状态 | `uiStore.locale`，持久化 `localStorage.locale` |
| 翻译调用 | `useI18n().t(key, params)` 或 `translate(locale, key, params)` |
| 参数插值 | `{name}` 占位符 |
| 回退策略 | 当前 locale → zh-CN → 原 key |
| 切换入口 | TopBar 用户菜单 |

### 1.2 翻译键完整性

| 指标 | 结果 |
|------|------|
| zh-CN 键数 | **987** |
| en-US 键数 | **987** |
| 仅 zh 有 | **0** |
| 仅 en 有 | **0** |

✅ **locale 文件完全对称，无缺失键。**

附带：`types.ts:509,515` 两行缩进异常（`|   'key'`），不影响运行时，建议格式化统一。

### 1.3 视图覆盖率

| 视图/模块 | useI18n | 状态 |
|-----------|---------|------|
| ChatView / BoardView / TaskTreeView | ✅ | 完整 |
| GanttView / CalendarView / WhiteboardView | ✅ | 完整 |
| FilesView / CockpitView / SetupWizard | ✅ | 完整 |
| DiscoverModal / ProfileModal / AiAssistantShell | ✅ | 完整 |
| GroupView（路由壳） | ✅ | 完整 |
| MainLayout / ViewHeader | — | 纯布局，文案由子组件承担（合理） |
| RootErrorBoundary / App.tsx | `translate()` 直调 | 合理 |

✅ **所有主视图均已接入 i18n。**

### 1.4 硬编码中文（locales 外）

| 严重度 | 文件:行 | 内容 | 说明 |
|--------|---------|------|------|
| **High** | `src/shared/cockpit/aiProviders.ts:15-59` | `DeepSeek（推荐）`、`通义千问 · DashScope`、`智谱 GLM`、`自定义` 等 | `AiConfigModal` Select 直接展示；切 en-US 仍为中文 |
| **Medium** | `src/shared/cockpit/aiProviders.ts:19,26,33,40` | `sk-...（platform.deepseek.com）` 等占位符 | API Key 占位符未本地化 |
| **Medium** | `src/renderer/src/plugin/builtins/formJsDemoSchema.ts:7,13,18` | `摘要`、`备注`、`已确认` | FormJs POC 直接渲染 `c.label` |
| **Low** | `src/renderer/src/platform/browserLanpmStub.ts` | `开发预览`、`预览用户`、`LanPM 协作组` 等 | 浏览器开发桩，非生产路径 |
| **Low** | `src/renderer/src/bootstrap.tsx:11` | `#root 不存在` | 启动失败极少触发 |
| **Low** | `src/renderer/src/i18n/groupLabels.ts:30-34` | `'私聊'` 字符串比较 | DB 种子兼容，非 UI 文案 |

### 1.5 硬编码英文 UI

| 严重度 | 文件:行 | 内容 | 建议 |
|--------|---------|------|------|
| **Medium** | `src/renderer/src/features/cockpit/AiConfigModal.tsx:121` | `label="Base URL"` | 新增 `ai.baseUrl` 键 |
| **Medium** | `src/renderer/src/features/chat/TaskSuggest.tsx:28` | `aria-label="Task suggestions"` | 新增 `chat.taskSuggestAria` |
| **Medium** | `src/renderer/src/plugin/PluginErrorBoundary.tsx:29` | `Plugin failed: {pluginId}` | 新增 `plugin.crashTitle` 等 |
| **Low** | `src/shared/cockpit/aiProviders.ts:43-52` | `OpenAI`、`Anthropic` | 品牌名，可保留 |

### 1.6 i18n 结论

- ✅ 键对称、主视图覆盖完整、类型安全
- ⚠️ **en-US 体验在 AI 配置、FormJs POC 处有中文泄漏**
- 优先修复：`aiProviders.ts` → `AiConfigModal` Base URL → `TaskSuggest` aria-label → `PluginErrorBoundary`

---

## 2. 视觉一致性审查

### 2.1 设计系统 SSOT

| 层级 | 路径 | 作用 |
|------|------|------|
| CSS 变量 | `src/renderer/src/styles/global.css` | `--lanpm-*` 颜色/字体/圆角/阴影；`html[data-theme]` 切换 |
| TS 实色 | `src/shared/design/lanpmDesignTokens.ts` | Ant ThemeProvider / 甘特 / 头像 |
| 主题桥接 | `src/renderer/src/app/ThemeProvider.tsx` | ConfigProvider + `dataset.theme` |
| 运行时读变量 | `src/renderer/src/ui/cssVar.ts` | SVG / 第三方组件 |
| 样式方案 | **CSS Modules** 为主 | 无 Tailwind / styled-components |
| 静态守卫 | `tests/static/verify-visual.ts` | V-01~V-14、CK-4xx、VP-4xx |

**字号令牌：** caption 11px · body 13px · title 17px · display 28px  
**圆角令牌：** sm 8 · md 12 · lg 16 · xl 20  
**间距：** 无 `--lanpm-spacing` 令牌，散落 magic number（8/10/12/14/16/48）

### 2.2 字号不一致

| 严重度 | 文件:行 | 问题 | 期望 |
|--------|---------|------|------|
| **Medium** | `ui/ViewHeader.module.css:19` | 页面标题 **22px**，偏离 title(17)/display(28) | 统一用令牌 |
| **Medium** | `features/tree/TaskDetailPanel.tsx:563,687` 等 | inline `fontSize: 11/12` | 进 module.css + 令牌 |
| **Medium** | `features/discover/DiscoverModal.tsx:293` | inline 24px 配对码 | display 或 title 令牌 |
| **Low** | `layout/GlobalSearch.module.css:88` | 11px 字面量 | `--lanpm-font-caption` |
| **Low** | `views/CockpitView.module.css:248` | chevron 12px | `--lanpm-font-body-secondary` |

`verify-visual.ts` 对 board/gantt/chat 等禁止 `font-size: Npx`（VIS-07b），但**未覆盖** Cockpit、Setup、aiAssistant、ViewHeader。

### 2.3 间距 / 边框 / 圆角不一致

| 严重度 | 问题 | 示例 |
|--------|------|------|
| **Medium** | 岛/面板内边距三套并存 | Cockpit `12/14/16px` vs feature 视图 `10px 12px` |
| **Medium** | 分隔线 0.5px + separator vs 1px + border 混用 | `CockpitView.module.css:166` vs `board.module.css:48` |
| **Medium** | Setup/plugin 圆角字面量 8px/12px/14px | 不在 sm/md/lg 阶梯 |
| **Medium** | 幻影变量 `--lanpm-radius-panel`（未定义，回退 8px） | `whiteboard.module.css:42` |
| **Low** | Pill 圆角 999px vs RegionButton 980px | 多处 |

### 2.4 颜色 / 主题不一致

| 严重度 | 文件:行 | 问题 |
|--------|---------|------|
| **High** | `src/renderer/src/main.tsx:13` vs `uiStore.ts:6-11` | 首屏主题：`main.tsx` 仅 `localStorage === 'dark' ? dark : light`，**忽略系统偏好**；uiStore 会读 `prefers-color-scheme` → **FOUC 风险** |
| **High** | `features/whiteboard/WhiteboardView.tsx:233` | `viewBackgroundColor: '#ffffff'` 固定白底，暗色模式视觉断裂 |
| **High** | `features/ai/SubtaskPreviewModal.tsx:93` | 幻影变量 `--lanpm-border-subtle` + `#e8e8e8` 回退，暗色露馅 |
| **Medium** | `features/discover/DiscoverCoachmark.tsx:37` | 遮罩 `rgba(0,0,0,0.45)`，非 `--lanpm-overlay` |
| **Medium** | `features/whiteboard/whiteboard.module.css:30` | Excalidraw 浮钮阴影硬编码 rgba |
| **Low** | `features/task/TaskTagChip.tsx:25` | 标签文字 `#fff`（动态背景场景可接受） |

### 2.5 组件复用不一致

| 组件 | 问题 | 严重度 |
|------|------|--------|
| **按钮** | `RegionButton`（壳层 32px）与 Ant `Button`（controlHeight 40）混用；Cockpit 内 16 处 Ant Button | **Medium** |
| **输入** | GlobalSearch 覆写 Ant Select 为 32px 药丸，与 ThemeProvider 40px 不一致 | **Medium** |
| **卡片/岛** | 无统一 Card 组件；Cockpit/Board/Calendar 各自实现相似岛式面板 | **Low–Medium** |
| **样式混用** | CSS Modules + Ant + inline `style={{}}` 三层并存；约 22 个 TSX 含 inline style | **Medium** |

### 2.6 verify:visual 守卫

- ✅ 覆盖：双主题令牌、禁止 Ant 遗留色、布局尺寸契约、七页视图令牌引用
- ⚠️ 本次审查环境未构建产物，`npm run verify:visual` 在 `global-*.css` 产物检查处失败（需先 `npm run build`）
- **缺口：** ViewHeader 22px、Cockpit/AI `font-size: Npx`、幻影 var 在 TSX inline、对比度/opacity 门禁

### 2.7 视觉一致性结论

设计令牌体系成熟，`verify:visual` 守卫覆盖面广。主要问题：**首屏主题分叉**、**白板暗色白底**、**幻影 CSS 变量**、**Ant 与壳层控件尺寸不统一**。

---

## 3. 文字对比度审查

### 3.1 核心色值与 WCAG 对比度（亮色主题）

背景 `#f5f5f7`（`--lanpm-bg`）：

| 文字色 | 色值 | 对比度 | WCAG AA（正文 ≥4.5:1） |
|--------|------|--------|------------------------|
| `--lanpm-text` | `#1d1d1f` | **15.46:1** | ✅ 通过 |
| `--lanpm-text-secondary` | `#6e6e73` | **4.66:1** | ✅ 通过 |
| `--lanpm-text-tertiary` | `#8e8e93` | **2.99:1** | ❌ **失败** |
| tertiary on `#fff` | — | **3.26:1** | ❌ 失败（正文级） |

暗色主题 tertiary on `#000`：**6.44:1** ✅ 通过。

**根因：** `global.css:69,130` 的 `--lanpm-text-tertiary: #8e8e93` 在亮色下不达标，但被大量用于 10–13px 正文级文字。

### 3.2 高影响对比度问题

#### H1 — 三级文字色 token 全局不达标（High）

| 文件:行 | 用途 | 视图 |
|---------|------|------|
| `layout/BottomNav.module.css:50` | 未选中 Tab 标签+图标 | 底栏全视图 |
| `layout/TopBar.module.css:103,317,324` | 顶栏按钮、群选择器 | 全局顶栏 |
| `layout/GlobalSearch.module.css:62` | 搜索占位/提示 | 全局搜索 |
| `features/chat/chat.module.css:1346` | 发送钮 disabled 文字 | 聊天 |
| `features/board/board.module.css:220` | 卡片菜单图标 | 看板 |
| `features/ai/aiAssistant.module.css:222,231` | 消息元信息 | AI 助手 |
| `features/gantt/gantt.module.css:12` | disabled 导出按钮 | 甘特 |
| `plugin/plugin.module.css:38` | 插件描述 | 扩展面板 |

**建议：** 亮色 tertiary 加深至 `#6e6e73` 或 `#75757a`，或限制 tertiary 仅用于 ≥18px 装饰性文字。

#### H2 — 二级色 + opacity 叠加（High）

| 文件:行 | 样式 | 有效对比度（估） | 组件 |
|---------|------|------------------|------|
| `features/chat/chat.module.css:749-751` | `.mentionOptionId` secondary + `opacity: 0.55` | ~**2.2:1** | TaskSuggest 状态标签 |
| `features/chat/chat.module.css:1344-1349` | `.sendIconBtn:disabled` tertiary + `opacity: 0.6` | ~**1.8:1** | 聊天发送禁用态 |
| `features/board/board.module.css:413-416` | `.legendDivider` tertiary + `opacity: 0.6` | ~**1.8:1** | 看板图例分隔符 |
| `features/chat/CodeSendModal.tsx:68` | inline `opacity: 0.65` 12px | ~**2.2:1** | 代码发送弹窗 |
| `styles/scheduleHealth.module.css:37-39` | `.dueOverdue` 用 tertiary 表示逾期 | **2.99:1** + 语义错误 | 任务树/看板 |

#### H3 — 聊天上下文文字偏淡（High / Medium）

| 严重度 | 文件:行 | 样式 |
|--------|---------|------|
| **High** | `features/chat/chat.module.css:810-814` | `.chatContextMeta` secondary + `opacity: 0.82` |
| **Medium** | `features/chat/chat.module.css:1053-1055` | `.status` caption + `opacity: 0.75` |

### 3.3 中等影响（Medium）

| 文件:行 | 问题 |
|---------|------|
| `layout/BottomNav.module.css:110-112` | `.tabDisabled` 整体 `opacity: 0.4` |
| `ui/ViewSegment.module.css:72-74` | disabled tab `opacity: 0.4` |
| `ui/regionInteract.module.css:44-46` | `.regionDisabled` `opacity: 0.4` |
| `features/board/board.module.css:119-120` | `.cardRelationDimmed` 整卡 `opacity: 0.42` |
| `features/tree/tree.module.css:278-280` | `.discussionKind` `opacity: 0.7` |
| `features/ai/aiAssistant.module.css:352-354,396-398` | disabled 按钮/芯片 `opacity: 0.45` |
| `features/calendar/calendar.module.css:65-66` | 推断日程 `opacity: 0.72` |

### 3.4 各主视图可读性小结

| 视图 | 风险等级 | 主要问题 |
|------|----------|----------|
| **聊天** | High | mention 状态、上下文条、发送禁用、送达状态 |
| **看板** | High / Medium | 卡片菜单 tertiary、图例 divider、关联 dim |
| **AI 助手** | High | 消息时间/元信息 tertiary、disabled 控件 |
| **底栏 / 顶栏** | High | 未选中 Tab / 操作钮 10px tertiary |
| **甘特** | High（亮） | disabled 导出钮 |
| **日历** | Medium | 推断事件半透明 |
| **驾驶舱 / 文件** | Low | 以 secondary 为主，基本 OK |

### 3.5 对比度结论

未发现 Tailwind `text-gray-400` 等；项目统一用 CSS Modules + `--lanpm-*`。  
**系统性问题：** 亮色 `--lanpm-text-tertiary` 不达标 + 多处 opacity 叠加使有效对比度更低。  
**一次修复 tertiary 可连带改善底栏/顶栏/聊天/看板/AI 等多处。**

---

## 4. 死代码审查

### 4.1 零引用源文件（High）

| 路径 | 证据 | 建议 |
|------|------|------|
| `src/renderer/src/features/chat/MentionText.tsx` | 全仓无 import；功能已由 `ChatMessageText.tsx` 承接 | **删除** |
| `src/renderer/src/ui/RegionTabBar.tsx` + `RegionTabBar.module.css` | 全仓无 import；`DmSessionBar` 已改用 `chat.module.css`；`verify-visual.ts:86` 仍守卫该文件 | **删除组件** + 同步更新 `verify:visual` 与 `docs/04` |

### 4.2 未使用导出（Medium）

| 路径:行 | 符号 | 证据 |
|---------|------|------|
| `src/renderer/src/features/files/fileListModel.ts:51` | `sortFiles()` | 仅定义；`FilesView.tsx` 用 Ant Table 内联 sorter |
| `src/renderer/src/features/task/scheduleHealthUi.ts:89` | `shouldShowScheduleHealthVisual()` | 仅定义，无调用方 |
| `src/shared/presence/display.ts:15` | `presenceLabel()` | 仅测试引用；UI 用 emoji + i18n（文档化保留） |

### 4.3 注释块 / 历史死代码

- ✅ 未发现大块注释掉的业务代码
- ✅ `notificationService.ts`、`appStore.ts` 等已在 SPRINT-09 删除（见 CHANGELOG）
- 空 catch `/* ignore */` 为有意为之（`browserLanpmStub.ts`、`SetupWizard.tsx` 等）

### 4.4 工具扫描

| 工具 | 状态 |
|------|------|
| `npm run lint` | 有 |
| `npm run typecheck` | 有 |
| `knip` / `ts-prune` | **无** npm script；临时 `npx knip` 有误报（verify 脚本、CLI bin 等实际在用） |

**knip 误报（勿删）：** `src/cli/main.ts`、`scripts/onekey-preflight.mjs`、`tests/static/verify-*.ts`

### 4.5 文档 / 守卫漂移

| 项 | 问题 |
|----|------|
| `docs/04_交互与UI约定.md` | 写明 DM 会话用 `RegionTabBar`，实现已偏离 |
| `tests/static/verify-visual.ts:86` | 守卫未使用的 `RegionTabBar` |

### 4.6 死代码结论

整体干净，主要遗留 **2 个零引用组件文件** + **2 个未使用导出函数**。建议删除并同步更新守卫与文档。

---

## 5. 优先修复路线图（2026-07-31 对账）

> **v1.57 quality-audit** 已交付 P0 全项 + P1 主体 + P2 多项；**v1.59～v1.61** IslandPanel 岛面；**v1.63 audit-polish** 清尾本节剩余可执行项。

### P0 — 已完成 ✅

| # | 项 | 状态 |
|---|-----|------|
| 1 | 统一首屏主题 `readInitialTheme` | ✅ v1.57 |
| 2 | 亮色 `--lanpm-text-tertiary` 加深 | ✅ v1.57 |
| 3 | 删除 `MentionText` / `RegionTabBar` | ✅ v1.57 |

### P1 — 已完成 ✅

| # | 项 | 状态 |
|---|-----|------|
| 4 | AI 提供商预设 i18n | ✅ v1.57 |
| 5 | 白板暗色背景适配 | ✅ v1.57（运行态 `readCssVar`） |
| 6 | 幻影 `--lanpm-border-subtle` | ✅ v1.57 |
| 7 | chat/board opacity 叠加 | ✅ v1.57 |

### P2 — 对账

| # | 项 | 状态 |
|---|-----|------|
| 8 | Base URL / TaskSuggest aria / PluginErrorBoundary i18n | ✅ v1.57 |
| 9 | ViewHeader 字号令牌化 | ✅ v1.57 |
| 9b | Cockpit Ant `Button` → `RegionButton` 统一 | ✅ v1.64 |
| 10 | 扩展 `verify:visual` | ✅ 多轮（CK/VP/UC/IS/AP） |
| 11 | 删 `sortFiles` / `shouldShowScheduleHealthVisual` | ✅ v1.57 |
| 12 | `knip` 进 devDeps + **verify:m7** | ✅ v1.63 |
| — | DiscoverCoachmark `--lanpm-overlay` | ✅ v1.63 |
| — | 白板浮钮 `--lanpm-shadow-island` | ✅ v1.63 |
| — | 白板种子 `LANPM_SURFACE_SOLID_HEX` | ✅ v1.63 |
| — | TaskDetailPanel inline `fontSize` | ✅ v1.63 |
| — | 间距令牌 / IslandPanel 岛面 | ✅ v1.59～v1.61 |

### 仍可选 polish（非 Blocker）

- GlobalSearch 32px vs Ant 40px 控件高度统一
- §3.3 对比度：`tabDisabled` / `cardRelationDimmed` 等语义性 `opacity`（产品接受度）
- `browserLanpmStub` / `bootstrap` 开发路径文案
- `types.ts` locale 缩进格式化

---

## 6. 审查方法说明

1. **i18n：** 对比 `zh-CN.ts` / `en-US.ts` 键集合；grep 中文/英文硬编码；逐视图检查 `useI18n` 使用
2. **视觉：** 分析 `global.css` 令牌体系；对比 views/features CSS Modules；对照 `verify-visual.ts` 守卫范围
3. **对比度：** 基于 `--lanpm-text*` 色值计算 WCAG 对比度；grep `text-tertiary` + `opacity` 叠加模式
4. **死代码：** grep import 引用链；`npx knip` 辅助扫描；对照 CHANGELOG 历史清理记录

---

## 7. 任务完成情况

| 任务 | 状态 |
|------|------|
| 国际化完整性审查 | ✅ 完成 |
| 视觉一致性审查 | ✅ 完成 |
| 各页面文字对比度审查 | ✅ 完成 |
| 死代码审查 | ✅ 完成 |
| 写入 `审查.md` | ✅ 完成 |
| **代码修复（P0/P1 + P2 主体）** | ✅ **v1.57 quality-audit** |
| **岛面 / 间距令牌** | ✅ **v1.59～v1.61** |
| **审查清尾（knip·m7 / overlay / 白板 / TaskDetail）** | ✅ **v1.63 audit-polish** |
| **knip 固化进 CI 链** | ✅ `verify:m7` 含 `npm run knip` |
| **verify:visual 全绿（需 build）** | ✅ 当前链可绿 |
| **遗留（可选下一 Sprint）** | 对比度 opacity 全站 · GlobalSearch 高度 |

---

*本报告由静态审查生成，对比度数值基于 sRGB 相对亮度公式估算；实际显示效果受显示器、字体渲染影响，建议在真机/多主题下人工抽检确认。*
