# 专项审查：异常值 / 边界值 / 默认值

| 项 | 内容 |
|----|------|
| 日期 | 2026-07-11 |
| 回写 | 2026-07-12 · **B5**（TASK-320–325）· **v1.26.0**；**B6**（TASK-330–335）· **v1.27.0** |
| 范围 | `src/shared` · `src/main` · `src/preload` · `src/renderer`（含 bundle / sync_outbox / 插件） |
| 方法 | **review** skill · review agent 只读扫 + 主 Agent 核对 `file:line` |
| 结论（原） | **需加固** |
| 结论（现） | **Blocker + Major + 本审查 Minor 已闭环** |

非完整 PR review；聚焦数值、空值、枚举、默认策略与边界截断是否合理。

---

## 处置状态

| # | 严重度 | 类别 | 状态 | 落地 |
|---|--------|------|------|------|
| 1 | Blocker | 边界值 | ✅ | TASK-320 最新优先导出 + 截断元数据/UI |
| 2 | Major | 异常值 | ✅ | TASK-321 导入 `db.transaction` |
| 3 | Major | 默认值/枚举 | ✅ | TASK-321 `assertBundleConflictMode` + IPC |
| 4 | Major | 边界值 | ✅ | TASK-322 `new_id` FK 重映射 |
| 5 | Major | 异常值 | ✅ | TASK-323 progress clamp 全路径 |
| 6 | Major | 默认值 | ✅ | TASK-324 默认拒绝 + 官方白名单 |
| 7 | Major | 边界值 | ✅ | TASK-324 `resolveLanpmTcpPort` |
| 8 | Minor | 默认值 | ✅ | TASK-321 同文件 `assertBundlePassword`（≥4） |
| 9 | Minor | 边界值 | ✅ | TASK-330 `clampTransferStartOffset` |
| 10 | Minor | 异常值 | ✅ | TASK-333 chat sync `catchSyncFailure` |
| 11 | Minor | 边界值 | ✅ | TASK-331 `FILE_TRANSFER_RATE_MAX_KBPS` |
| 12 | Minor | 默认值 | ✅ | TASK-332 `DEFAULT_GROUP_AUTO_DISCOVER=false` |
| 13–15 | Minor | 混合 | ✅ | TASK-334 outbox limit clamp；其余同轮闭合 |
| 16–20 | Info | 正面 | — | 保留为范本 |

---

## 原发现列表（归档留痕）

### 1–7 · Blocker / Major · ✅（B5）

见 v1.26.0 CHANGELOG；细节略。

### 8–15 · Minor · ✅（B6）

| 项 | 落地 |
|----|------|
| 续传 `startOffset` | `clampTransferStartOffset` · `tests/unit/file/transferOffset.test.ts` |
| 限速无上限 | `clampFileTransferRateKbps` · max `100_000` |
| `autoDiscover` 默认 true | `DEFAULT_GROUP_AUTO_DISCOVER=false` + 表单 hint |
| sync `catch` 裸吞 | `catchSyncFailure` · `verify:chat-sync-observability` |
| outbox `limit` | `clampSyncOutboxListLimit` · 默认 50 / 上限 500 |

### 16–20 · Info（正面）

`clampProgressPercent` · retention MIN/MAX · outbox 退避 · 冲突默认 skip · linkedFiles 上限 — 仍作范本。

---

## 交付走查（TASK-335 · B6）

| 维 | 结果 |
|----|------|
| i18n | `group.autoDiscoverHint` 中英齐全；`verify:i18n-keys` 绿 |
| 自动化 | lint（0 error）· typecheck · test:unit · verify:sync-outbox · verify:chat-sync-observability 绿 |
| 文档 | README · CHANGELOG **1.27.0** · docs ROADMAP 对齐 |
| Blocker | **无** |

---

## 手验收口（Sprint `review-handverify-close`）

> 无交互 Electron 会话时，以 **代码绑定 + 单测** 代理手验（与 plan DOC-342 允许「跳过+单测覆盖」同口径）。

| ID | 项 | 结果 | 证据 |
|----|-----|------|------|
| DOC-340 | 新建群发现默认关 | **通过** | `DEFAULT_GROUP_AUTO_DISCOVER=false`（`src/shared/group/types.ts`）；`CreateGroupModal` open 时 `setFieldsValue({ autoDiscover: DEFAULT… })`；`vitest` `autoDiscoverDefault.test.ts` 绿（2026-07-12） |
| DOC-341 | 限速超大值夹紧 | ⬜ | — |
| DOC-342 | 续传异常 offset 从头传 | ⬜ | — |

---

## 审查元数据

- 路由：`/master` → **review** → `/plan` B5→B6 → `/run` TASK-320–325 · TASK-330–335
- 原对照 agent：[边界值默认值审查](66fb2223-c50a-42a0-96cc-b4c885079345)
