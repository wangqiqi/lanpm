# 专项审查：异常值 / 边界值 / 默认值

| 项 | 内容 |
|----|------|
| 日期 | 2026-07-11 |
| 回写 | 2026-07-12 · **B5-boundary-harden**（TASK-320–325）· **v1.26.0** |
| 范围 | `src/shared` · `src/main` · `src/preload` · `src/renderer`（含 bundle / sync_outbox / 插件） |
| 方法 | **review** skill · review agent 只读扫 + 主 Agent 核对 `file:line` |
| 结论（原） | **需加固** |
| 结论（现） | **Blocker + Major 已闭环**；Minor 进候选「边界值 Minor 收尾」 |

非完整 PR review；聚焦数值、空值、枚举、默认策略与边界截断是否合理。

---

## 处置状态（B5）

| # | 严重度 | 类别 | 状态 | 落地 |
|---|--------|------|------|------|
| 1 | Blocker | 边界值 | ✅ | TASK-320 最新优先导出 + 截断元数据/UI |
| 2 | Major | 异常值 | ✅ | TASK-321 导入 `db.transaction` |
| 3 | Major | 默认值/枚举 | ✅ | TASK-321 `assertBundleConflictMode` + IPC |
| 4 | Major | 边界值 | ✅ | TASK-322 `new_id` FK 重映射 |
| 5 | Major | 异常值 | ✅ | TASK-323 progress clamp 全路径 |
| 6 | Major | 默认值 | ✅ | TASK-324 默认拒绝 + 官方白名单 |
| 7 | Major | 边界值 | ✅ | TASK-324 `resolveLanpmTcpPort` |
| 8–15 | Minor | 混合 | ⬜ 候选 | 密码已在 #3 同文件落地；其余见 plan 候选「边界值 Minor 收尾」 |
| 16–20 | Info | 正面 | — | 保留为范本 |

---

## 原发现列表（归档留痕）

### 1 · Blocker · 边界值 · ✅

- **位置**：`src/main/data/bundleService.ts` · `messageRepository`（原 ASC+LIMIT）
- **发现**：导出消息 `ASC + LIMIT` 静默丢掉最新。
- **落地**：`listMessagesForBundleExport` DESC 取最近 N 条；`messagesTruncated` + UI warning。

### 2 · Major · 异常值 · ✅

- **发现**：导入无事务 → 半导入。
- **落地**：整包 `db.transaction`；verify 触发器回滚用例。

### 3 · Major · 默认值 / 枚举 · ✅

- **发现**：`conflictMode` 未白名单；IPC 密码过弱。
- **落地**：`assertBundleConflictMode` · `assertBundlePassword`（≥4）。

### 4 · Major · 边界值 · ✅

- **发现**：`new_id` 未重映射外键。
- **落地**：预建 task/msg/file map，重写 parent/linked/source/reply 等。

### 5 · Major · 异常值 · ✅

- **发现**：创建路径未 clamp 进度。
- **落地**：insert/build/rowToTask/aggregate/树内联统一 clamp。

### 6 · Major · 默认值 · ✅

- **发现**：插件未配置默认 `true`。
- **落地**：默认拒绝；`lanpm.example` / `lanpm.formjs` 白名单默认启用。

### 7 · Major · 边界值 · ✅

- **发现**：`LANPM_TCP_PORT` 无 1–65535 校验。
- **落地**：`resolveLanpmTcpPort` 非法回退默认并 warn。

### 8–15 · Minor · 未本轮（候选）

续传 `startOffset` 负数 · 限速无上限 · `autoDiscover` 默认 true · sync `catch` 吞异常 · outbox `limit` 未 clamp 等 — 见 `.cursorGrowth/plan.md` 候选表。

### 16–20 · Info（正面）

`clampProgressPercent` · retention MIN/MAX · outbox 退避 · 冲突默认 skip · linkedFiles 上限 — 仍作范本。

---

## 交付走查（TASK-325 · 数据设置 / 备份 / 插件）

| 维 | 结果 |
|----|------|
| i18n | `data.bundleExportTruncated` 中英齐全；`verify:i18n-keys` 绿 |
| 文档 | README · CHANGELOG 1.26.0 对齐 |
| Blocker | **无**（自动化 VERIFY 绿；手验 Profile→数据备份 / 扩展启停建议发版前点一次） |

---

## 审查元数据

- 路由：`/master` → **review** → `/plan` B5 → `/run` TASK-320–325
- 原对照 agent：[边界值默认值审查](66fb2223-c50a-42a0-96cc-b4c885079345)
