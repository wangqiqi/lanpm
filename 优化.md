# LanPM 产品界面 · 用户心智负担评审

> **评审日期**：2026-07-30  
> **范围**：桌面端 Renderer 主路径（首次启动 · 顶栏 · 发现/配对 · 群组内导航）  
> **依据**：`docs/01_产品需求文档.md` · `docs/配对码.md` §3–4 · 源码 `DiscoverModal` / `DiscoverPairingPanel` / `TopBar` / `SetupWizard` / `BottomNav`  
> **方法**：任务路径步数统计 · 概念重叠分析 · 与 PRD/配对码设计稿对照（未做可用性实测）

---

## 1. 总体结论

| 维度 | 评价 | 说明 |
|------|------|------|
| **核心 PM 路径**（已有群组后看板/任务） | 中等 | 底栏 7 Tab 功能完整，但学习成本高 |
| **组网/入群路径**（发现 · 配对 · 种子） | **偏高** | 单弹窗承载概念多、入口分散、成功配对后仍要二次操作 |
| **首次使用** | **偏高** | 无群组时进驾驶舱，与「先组网再加入」心智不一致 |
| **网络异常恢复** | 中等偏高 | 「手动节点」在 4 处重复出现，用户不知选哪 |

**一句话**：产品能力已对齐 PRD，但 **「发现」弹窗 + 顶栏网络** 仍是主要认知税；优化重点应是 **减概念、缩路径、情境化默认**，而非再加功能。

---

## 2. 用户心智模型（产品希望 vs 界面实际）

### 2.1 产品希望的心智（`配对码.md` §3）

```
同网段：码 → 看到群 → 加入
跨网段：码（+ 必要时 IP/尾段）→ 看到群 → 加入
```

用户脑中往往是：**「连上同事」≈「进他的项目群」**，一步完成。

### 2.2 界面实际心智

```
配对码连接  →  仅同步「可发现群组目录」+ 建立 P2P
              →  仍需在「群组」Tab 对每个群「申请加入」或再用「群邀请码」
```

**断层**：配对成功文案为「已连接 {name}，发现 {count} 个群组」（`discover.pairingJoinSuccess`），用户易理解为「已经连上项目」，实际只是 **发现层连通**，入群是第二步。这是当前 **最大的心智负担来源**。

---

## 3. 关键旅程 · 操作步数

| 旅程 | 理想步数（用户预期） | 当前步数 | 差距 |
|------|---------------------|----------|------|
| 同网段新人加入同事项目 | 2–3（打开 → 输码 → 进项目） | **6–8** | 见 §3.1 |
| 同网段分享方拉人 | 2（分享码 → 对方输入） | 3–4 | 可接受 |
| 跨网段加入 | 3–4（码 + 可选 IP） | 6–9 | 高级项折叠较好，但概念仍多 |
| VPN/发现失败补救 | 1（填 IP:端口） | **2–4**（需先找到正确入口） | 见 §4.2 |
| 首次安装无群 | 1 步引导组网 | **0 步引导**（进驾驶舱） | 见 §3.2 |

### 3.1 加入方典型路径（同网段）

1. 顶栏点 **「发现」**
2. 点 **「用连接码查找群组」**（`idle` → `find` 模式切换）
3. 输入 6 位码 → **「连接」**
4. 切到 **「群组」Tab**（配对成功后会切 Tab，但列表可能多条）
5. 对目标群点 **「申请加入」**（或先去「高级」找群邀请码）
6. 等待对方审批（若需治理）
7. 再点 **「进入」** 才开始用看板/聊天

**可优化点**：步骤 4–6 在「仅 1 个可加入群」时已有 `singleJoinableGroup` 快捷条（`DiscoverModal` L323–339），但 **配对成功瞬间未自动触发申请**；多群时负担仍大。

### 3.2 首次启动（零群组）

`HomeRedirect`：无群组 → **驾驶舱**（`/cockpit`），而非发现/配对引导。

- 驾驶舱面向 **主管/跨项目**，新人更需「如何加入第一个群」
- Setup 网络页可 **跳过**，跳过后无「下一步去发现」的 CTA

**心智冲突**：副标题写「加入局域网协作」，落地页却是驾驶舱仪表盘。

---

## 4. 分区评审

### 4.1 发现弹窗（`DiscoverModal`）— 负担：**高**

**单屏同时呈现**：

| 区块 | 用户需理解的概念 |
|------|------------------|
| 健康 Alert | UDP/组播/离线等 7 种 reason |
| `DiscoverPairingPanel` | 分享码 / 查找码 / 导入导出配对文件 |
| 单群快捷 Alert | 条件出现 |
| 入群申请列表 | 审批流 |
| Collapse「高级」 | 发现种子 · **群邀请码** · 手动节点 |
| Tabs | 群组列表 · 成员私聊 |

**三种「码」并存**（用户极易混淆）：

| 名称 | 用途 | UI 位置 |
|------|------|---------|
| **群组连接码** | 配对、同步发现目录 | 顶部主按钮 |
| **群邀请码** | 免审批直入某群 | 高级折叠内 |
| （种子里的地址） | 长期重连 | 高级折叠内 |

建议文案层做 **「连接码 = 找同事」「邀请码 = 进某个群」** 的并排对照，而非都放在「发现」里用不同层级藏。

**与设计稿差距**（`配对码.md` §4.2–4.3）：

| 设计稿 | 实现 | 影响 |
|--------|------|------|
| 「复制配对信息」 | ❌ 未实现 | 有 IM 的场景要多口述码+IP |
| 加入方 IP 在勾选跨网段后 **同层展示** | IP 在 **二级 Collapse「高级」** 内 | 跨网段兜底多 1–2 次点击 |
| 高级项收拢为「手动 IP / 种子 / 配对文件」 | idle 态仍展示 **导入/导出配对文件** 与主按钮并列 | idle 四按钮，选择成本高 |

**做得好的地方**：

- 跨网段默认 **只填码** + `pairingRouteHint` 说明路由引导（与 P1 能力一致）
- `singleJoinableGroup` 一键申请
- `NetworkHelpModal` 复用 Setup 图示

### 4.2 顶栏网络 vs 发现 — 负担：**中高**

| 入口 | 能力 |
|------|------|
| 顶栏 **网络圆点**（点击/下拉） | 刷新 · 重连 · **手动添加节点** |
| 发现弹窗健康区 / 高级区 | **手动添加节点**（关闭发现再开 ManualPeerModal） |
| 发现种子 | 保存 `IP:端口`，刷新时自动连 |

三者解决 **同一类问题**（UDP 不可达/VPN），但 **术语与入口不统一**：

- 「手动添加节点」≈ 一次性连接
- 「发现种子」≈ 持久地址簿
- 「配对码」≈ 带校验的首次握手

**用户不知道**：先配对还是先手动节点？种子和手动节点区别是什么？

### 4.3 顶栏主操作区 — 负担：**中**

同区域并列：**驾驶舱 · AI 助手 · 发现 · 创建群组 · 解散群组**（后两者条件显示）。

- **发现** 与 **创建群组** 对新用户都是「我要开始协作」— 未说明「先发现加入 vs 自己建群」
- **驾驶舱** 与 **群组内 PM** 是不同角色入口，平级曝光会增加「我现在该看哪个」的犹豫

### 4.4 底栏 7 Tab — 负担：**中**（熟手低、新手高）

`chat · board · tree · gantt · calendar · whiteboard · files`

- 产品定位 **PM 本体**，但 `HomeRedirect` 默认进 **chat**
- 功能群（`function`）有 Tab 锁定 + 一次性 Modal 说明，做法正确；**项目群** 无类似「默认着陆看板」引导

### 4.5 首次 Setup — 负担：**中**

- 网络说明页（图示：交换机/路由/热点）**信息量大**，可跳过 → 多数用户会跳
- 跳过后无 **「你还没有群组，点这里用连接码加入」** 的轻量引导

---

## 5. 优化建议（按优先级）

### P0 · 减概念 / 缩关键路径（建议下一 Sprint）

| # | 建议 | 预期效果 | 涉及 |
|---|------|----------|------|
| P0-1 | **配对成功后智能入群**：若 `joinableGroups.length === 1` 且无需审批 → 自动 `joinGroup` 并 `navigate`；若需审批 → 自动提交申请并 Toast 说明等待 | 加入路径 **6 步 → 3 步** | `DiscoverPairingPanel.handleFind` · `DiscoverModal` |
| P0-2 | **三种码一页说清**：发现弹窗顶部增加固定 2 行说明或 Segmented「我是分享方 / 我要加入」替代 idle 四按钮并列 | 降低码概念混淆 | `DiscoverPairingPanel` idle UI |
| P0-3 | **零群组首屏**：`groups.length === 0` 时优先 **发现引导空态**（或 Modal），而非默认驾驶舱 | 对齐「先协作入网」心智 | `HomeRedirect` 或 Cockpit 顶栏 CTA |
| P0-4 | **统一补救入口**：网络异常时仅在 **一处** 展示主 CTA（建议发现弹窗内），顶栏圆点下拉只保留「刷新/重连」，「手动节点」链到同一向导 | 减少 4 入口困惑 | `TopBar` · `DiscoverModal` |

### P1 · 渐进披露 / 减点击

| # | 建议 | 预期效果 |
|---|------|----------|
| P1-1 | 跨网段勾选后 **同层展示 IP/尾段输入**（与设计稿 §4.3 对齐），`subnetScan` 仍放高级 | 少 1 次展开 Collapse |
| P1-2 | 分享态增加 **「复制配对信息」**（码 + IP + 可选群名） | 补设计稿缺口 |
| P1-3 | idle 态 **收起** 导入/导出配对文件到「高级」或「无屏/机房」子入口 | idle 主路径只保留 2 按钮 |
| P1-4 | 配对 `find` 模式：**连接码输入框获得焦点**且支持粘贴 6 位自动提交 | 少 1 次点击 |
| P1-5 | Setup 跳过后 **一次性** Coachmark 指向顶栏「发现」 | 首次路径闭环 |

### P2 · 角色化 / 长期

| # | 建议 |
|---|------|
| P2-1 | 项目群默认着陆 **看板**（可配置），与 PRD「PM 本体」一致 |
| P2-2 | 底栏按群组类型 **隐藏低频 Tab**（如匿名群已做，项目群可默认折叠 gantt/calendar） |
| P2-3 | 驾驶舱仅对「已有 ≥1 项目群」或主管角色突出；新人侧栏弱化 |
| P2-4 | 发现弹窗拆为 **向导式两步**（先配对连通 → 再选群加入），而非单页堆叠 |

---

## 6. 路径优化对照表（优化后目标）

| 场景 | 当前 | 目标（P0+P1 后） |
|------|------|------------------|
| 同网段加入单群 | 发现 → 查找码 → 连接 → 申请加入 → 进入 | 发现 → **加入**（输码）→ **自动进群主视图** |
| 跨网段加入 | 发现 → 查找 → 勾选跨网段 →（或展开高级）→ 连接 → … | 发现 → **加入** → 码 + **同层可选 IP** → 自动后续 |
| 发现失败 | 猜顶栏圆点 / 高级 / 健康条 | **单按钮「无法发现？按此连接」** 向导 |
| 首次无群 | 驾驶舱 | **加入协作** 空态 + 发现 |

---

## 7. 不建议做的「伪优化」

| 做法 | 原因 |
|------|------|
| 再增加顶栏图标入口 | 已拥挤，应先合并 |
| 默认展开全部高级项 | 会加重恐慌，应情境展开（仅 `health.suggestManualPeer` 时） |
| 去掉入群审批 | 治理需求真实存在；应自动化 **单群无审批** 分支，而非删除审批 |
| 把群邀请码提到与连接码完全同级 | 仍须区分场景；用 **文案与两步向导** 而非再多主按钮 |

---

## 8. 验收建议（若立项优化 Sprint）

| 指标 | 度量方式 |
|------|----------|
| 同网段「输码 → 进入群聊天/看板」步数 | ≤ 3（含打开发现） |
| 新用户 5 分钟内完成首次入群率 | 可用性测试 n≥5 |
| 「三种码」混淆率 | 任务后问卷：能否说清连接码 vs 邀请码 |
| 发现弹窗平均停留时间 | 配对成功前 ≤ 60s（日志/埋点） |

---

## 9. 评审文件索引

| 文件 | 关联结论 |
|------|----------|
| `src/renderer/src/features/discover/DiscoverModal.tsx` | §4.1 信息架构 |
| `src/renderer/src/features/discover/DiscoverPairingPanel.tsx` | §4.1 配对 UI · §5 P1-1/3 |
| `src/renderer/src/layout/TopBar.tsx` | §4.2 · §4.3 |
| `src/renderer/src/routes/HomeRedirect.tsx` | §3.2 · P0-3 |
| `src/renderer/src/layout/BottomNav.tsx` | §4.4 |
| `docs/配对码.md` §4 | 设计真源对照 |

---

*本文档为体验评审产出，非实施承诺；落地请走 `/plan` 拆 TASK 并做可用性回归。*
