# 项目检查报告（2026-05-28）

> 触发：继续推进 RC 前全面检查。版本：**1.0.0-rc.5**

---

## 1. 自动化结果

| 检查项 | 命令 | 结果 |
|--------|------|------|
| TypeScript | `npm run typecheck` | ✅（verify:m7 内含） |
| ESLint | `npm run lint` | ✅（修复 5 处 unused / prefer-const） |
| 生产构建 | `npm run build` | ✅ |
| 全量回归 | `npm run verify:m7` | ✅ Linux 本机 |
| 视觉守卫 | `npm run verify:visual` | ✅ |
| 全局搜索 | `npm run verify:search` | ✅（新纳入 verify:m7） |
| 项目健康 | `npm run verify:project` | ✅（版本与 CHANGELOG 一致 + lint + search） |

---

## 2. 本次修复

| 文件 | 问题 |
|------|------|
| `src/main/index.ts` | 未使用的 `refreshNetworkIdentity` 导入 |
| `RealNetworkTransport.ts` | 未使用的 `PEER_TTL_MS` 导入 |
| `peerLink.ts` | 未使用的 `generateDhKeyPair` 导入 |
| `browserLanpmStub.ts` | 未使用的 `MoveTaskInput` 类型导入 |
| `ganttAdapter.ts` | `prefer-const`（`start`） |
| `GanttView.tsx` | `useMemo` 依赖告警 → `useEffect` 刷新 `todayColor` |

---

## 3. 新增脚本

- **`npm run verify:project`**：package.json 与 CHANGELOG 版本对齐、docs/00 索引、`lint` + `verify:search`
- **`verify:m7`** 增加 `search` 步骤

---

## 4. 仍待人工（非阻断）

| 项 | 跟踪 |
|----|------|
| V-14b 亮/暗七页截图 | [docs/09](../docs/09_视觉手验清单.md) |
| Win / macOS 冒烟 | [todo.md](../todo.md) RC |
| 性能手测表（冷启动/内存/Tab P95） | [docs/08](../docs/08_M7_RC验收清单.md) §16.11 |
| 双机真网联调 | [docs/06](../docs/06_测试与联调手册.md) §4 |
| v1.1 任务 P2P、远端文件下载 | [todo.md](../todo.md) backlog |

---

## 5. 低优先级观察

- `topbar.profile` i18n 仍为英文 placeholder 文案（P1 个人设置页未做）
- `docs/08` 文首 RC 版本号仍写 `1.0.0-rc.1`，发版前需改为当前 RC
- 生产包 `index-*.js` ~4MB，后续可做 code-split（非 RC 阻断）

---

## 6. 建议下一步

1. 本机 `npm run dev` 完成 docs/09 手验 → 勾选 V-14b  
2. Win/macOS 各跑一轮 `verify:m7` + 启动冒烟  
3. 填 docs/08 性能表后发 **1.0.0**
