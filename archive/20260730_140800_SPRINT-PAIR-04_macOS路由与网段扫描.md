# SPRINT-PAIR-04 · macOS 路由 + `/24` 扫描

- **版本**：v1.46.0
- **日期**：2026-07-30
- **Goal**：补齐 macOS 路由解析；跨网段可选 `/24` 主机扫描（P2 高级兜底）。

## 交付物

| 项 | 路径 |
|----|------|
| macOS 路由 | `parseMacOsNetstatRn` · `routeTable.ts` darwin |
| 扫描列表 | `subnetScanHosts.ts` |
| 分批 lookup | `pairingUdp.lookupPairingCodeBatched` |
| 服务层 | `pairingService` · `subnetScan` / `subnetScanBatch` |
| UI/CLI | `DiscoverPairingPanel` 高级 · `lanpm pairing join --subnet-scan` |
| 验收 | `verify:pairing-subnet-scan` · 纳入 `verify:pairing-code` |

## 默认行为

- 网段扫描 **默认关**；勾选后先路由广播再分批扫描
- 并发 32 · 总上限 512 host · 批超时 800ms
