# 死代码 / 死文档审查

> **原仓库根 `无用.md`**。SPRINT-09/10 执行后迁入 `archive/`（2026-07-28）。  
> High/Medium 已闭合见 `CHANGELOG` `1.33.6` · SPRINT-09/10。

> 日期：2026-07-28 · 范围：`src/` · `scripts/` · `docs/` · `archive/` · `README*` · `plugins/`  
> 方法：入口链 + 全仓引用计数 + 文档导航/命令对照 · 只读审查（本稿不删改业务）  
> 严重度：**High** 高置信可处理 · **Medium** 需确认 · **Low** 可保留或文档化

---

## 摘要

| 类别 | High | Medium | Low |
|------|------|--------|-----|
| 死代码 | 2 | 5 | 若干保留项 |
| 死/过期文档 | 3 | 若干 | 若干 |

**优先清理**：删 2 个零引用源文件 → 修 README 插件表述 + `docs/05` 假命令 + `MIT开源替代` 过期表。

---

## 一、死代码

### High（可删）

| 路径 | 证据 | 建议 |
|------|------|------|
| `src/main/chat/notificationService.ts` | `notifyIfMentioned` 仅定义处 + CHANGELOG 旧述；运行时已走 Renderer `useChatNotifications` + `notification:show` IPC | **删除**整文件 |
| `src/renderer/src/stores/appStore.ts` | `useAppStore` 零业务引用；M1 占位，已被 `identityStore` / `navigationStore` 等替代 | **删除**整文件 |

### Medium（需确认）

| 路径 | 证据 | 建议 |
|------|------|------|
| `src/shared/task/boardRelations.ts`（`getFsBlockersForStatus`，约 L244） | `@deprecated`，全仓仅定义；生产用 `getDependencyBlockersForStatus` | **删除**该导出 |
| `plugins/lanpm.formjs/demo-schema.json` | 与 `formJsDemoSchema.ts` 重复；`FormJsPoc` 只读 TS；`plugin.json` 不引用 JSON | **删 JSON** 或定单一 SSOT |
| `tests/static/verify-storage-path-resolver.ts` | 有效但未挂 `package.json` / `verify:p0` / `verify:m7` | 接入 script **或**归档 |
| `src/shared/presence/display.ts`（`presenceLabel`） | 仅单测 / `verify:presence`；UI 用 emoji + i18n | 删导出 **或** 接入 UI |
| `scripts/build-icons.mjs` → `resources/logo-32.png` / `logo-64.png` | 生成后无代码/README 引用（UI 用 `logo.svg`） | 停止生成或文档化用途 |

### Low（建议保留）

| 路径 | 说明 |
|------|------|
| `scripts/upgrade-node-24.sh` | 运维一次性脚本，未挂 npm；保留并在 `docs/05` 注明即可 |
| `LEGACY_MESSAGE_TTL_META_KEY` / `profilePaths` legacy DB 迁移 | 数据兼容，勿删 |
| `ExampleStub` / `FormJsPoc` / `browserLanpmStub` | registry / `dev:web` / verify 守卫在用 |
| `onekey_run.*` | 活跃运维入口 |

### 构建产物（非源码死代码）

| 项 | 现状 |
|----|------|
| `dist/` · `dist-*`（含 `dist-pack*`） | `.gitignore` 已忽略 |
| `out/` · `coverage/` · `.lanpm/` | 已忽略 |
| `resources/icon.png` · `icon.ico` · `logo-*.png` | 生成物；可选补 gitignore + 注释「`npm run build:icons`」 |

### 易误判（非死）

入口链 `main/index.ts` → IPC → preload → `AppRouter` 七视图齐全；绝大多数 `verify:*` 目标文件存在且有调用链；根 `plugins/` 为 `extraResources` 来源。

---

## 二、死文档 / 过期文档

### High（应先修）

| 路径 | 证据 | 建议 |
|------|------|------|
| `README.zh-CN.md` L145 · `README.md` 对应行 | 上文已写 `verify:plugin-loader` / `plugin-enable-ui` 已交付，下文仍写「加载器尚未落地」 | **删改矛盾句** |
| `docs/05_测试与联调发布.md` ~L196 | `npm run build && npm run start` — `package.json` **无** `start`（有 `preview`） | 改为 `npm run preview` |
| `docs/MIT开源替代.md` §4.6 | 「插件 / App ❌」与已落地 loader + form-js POC 矛盾；§4.1 仍写「5 视图」实为七视图 | 更新为 ✅ + 链 `plugins/`；视图数改为 7 |

### Medium

| 路径 | 证据 | 建议 |
|------|------|------|
| ~~`评估.md`（根目录）~~ | **已归档** → `archive/20260728_192629_手工截图UI评估_评估.md`（SPRINT-04～07 收口） | — |
| `docs/飞鸽飞秋.md` | README 有链，但 `00` §1 正式清单未列（仅附录 MIT） | §1 增附录 B 或改为「6+2」 |
| `docs/飞鸽飞秋.md` §8 vs `MIT开源替代` | 开源候选表双份维护 | SSOT → MIT；飞鸽 §8 缩为外链 |
| `docs/飞鸽飞秋.md` §7/§10 vs `06_ROADMAP` §3 | 会议/可购插件叙述重复 | 排期只维护 ROADMAP |
| `README.zh-CN.md` ~L259 | 「进行中：视觉抛光 II」；CHANGELOG 1.32–1.33 已交付 VP | 从「进行中」移除或改「手验」 |
| `docs/01` §1.3 表 vs §1.3.1 | 表写 IndexedDB+SQLite；正文 RC 以 SQLite 唯一持久化 | 表与 §1.3.1 对齐 |
| `docs/飞鸽飞秋.md` | 链 `.cursorGrowth/...`（gitignore，仓库不可达） | 改 CHANGELOG / `docs/04` 或标「本地 Growth」 |

### Low

| 路径 | 建议 |
|------|------|
| README 文档表未列 `MIT开源替代` / `CHANGELOG` / `plugins/README` | 补链 |
| `docs/06_ROADMAP.md` 脚注「≥1.27.0」 | 改为以 `package.json`（当前 1.33.x）为准 |
| `docs/01` 「RC 实现现状」标题 | 改「实现现状」减少语义漂移 |
| `archive/*` | README **未**当 SSOT 硬链（合规）；归档文末勿复述现行索引 |
| `verify:meeting-plugin` / `plugins/lanpm.meeting` | ROADMAP 待补项，非断链 bug |

### 重复收敛（建议）

1. **实现态对照** → 只维护 `MIT开源替代.md`  
2. **未完成排期** → 只维护 `06_ROADMAP.md`  
3. **已交付叙事** → 只维护 `CHANGELOG.md`  
4. **飞鸽飞秋** → 竞品结论 + 外链，少复制表格

---

## 三、建议执行顺序（未执行）

1. 删 `notificationService.ts`、`appStore.ts` → 跑相关 typecheck / verify  
2. 删 `getFsBlockersForStatus`；统一 form-js demo SSOT  
3. 修 README 插件矛盾、`docs/05` `start`、`MIT开源替代` 插件/视图  
4. ~~归档根目录 `评估.md`~~ ✅；`00` 收录飞鸽；收敛飞鸽重复节  
5. 可选：`verify-storage-path-resolver` 入 CI；生成图标 gitignore

---

## 四、审查边界

- **未改**业务代码 / 文档正文（仅产出本报告）  
- **未扫** `.cursor/` SOP 母版作为「产品死文档」主体  
- **未扫** `node_modules` / 已忽略 `dist*` 内容  
- 引用守卫：`verify:docs-links` 主要扫 `docs/*.md` 相对链，**不**覆盖假 npm script、Growth 路径、根目录 `评估.md`
