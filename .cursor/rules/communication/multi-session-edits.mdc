---
description: 多会话并行 — 不得覆盖或恢复其他对话框的磁盘改动
alwaysApply: true
---

# 多会话编辑主权

用户可能同时开多个 Cursor 会话并行操作。**各会话互不知晓**；工作区磁盘是唯一共享状态。

## 禁止

- 发现文件与「本会话上次所见」不一致时，**不得**擅自：
  - `git checkout` / `git restore` / `git reset` 回滚该文件
  - 整文件 `Write` 覆盖
  - `StrReplace` 基于旧快照强行「纠正」
- **不得**把外来改动当成「错误」静默删除
- **不得**在未获用户确认时，恢复「本会话开始前的版本」

## 必须

1. **改前再读**：编辑目标文件前 `Read` 当前内容（大文件可读相关区段）
2. **只动本会话**：只撤销/回滚**本会话本轮**明确做过的修改
3. **外来差异 → 停手**：
   - 说明：哪些行/块不像本会话产出
   - 取证：`git diff <path>` 或 Read 对比
   - **AskQuestion**（不可用 → 正文编号选项）：保留外来并合并 / 跳过该文件 / 用户手动协调后再继续
4. **未解冲突不完成**：存在未解决冲突时，不得 ✅ TASK、不得 commit、不得声称 verify 通过

## 与会话内纠错的区别

| 场景 | 允许 |
|------|------|
| 本会话刚写错，现在改回 | ✅ 只动自己那几行 |
| 另一对话框刚改的同名文件 | ❌ 停手问用户 |
| verify 失败要修**本会话**引入的错 | ✅ |
| verify 失败但 diff 含外来块 | ❌ 先 AskQuestion |

## 配套

- 并行大改：**git worktree** 一会话一目录（**git** skill）
- Sprint 内：`plan.md` TASK **`Owns`** 列标明独占路径；重叠须串行或拆 worktree（**plan** skill）
