---
description: Before LanPM major release or RC, run visual gate (verify:visual, docs/06 §2.6)
alwaysApply: false
---

# 发版视觉门禁

当用户提到 **发版、RC、1.0.0、release、准备发布** 时：

- **打版 / 发布二进制**：`.cursor/rules/lanpm-release.mdc` + `lanpm-release` skill（A 日常 / B 出包）
- **A 打版**：`verify:visual`（改过 UI 时）+ `verify:m7`；不跑 `verify:release-gate`
- **B 发布二进制 / 仅 UI 全量门禁**：`lanpm-visual-audit` 门禁模式 + `verify:release-gate`（或至少 `verify:visual` + `docs/06` §2.6）
- 勿将 `.lanpm/visual-screenshots/` 提交入库
