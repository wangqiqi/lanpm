---
description: ROADMAP/archive/CHANGELOG 职责 · doc-coherence · 断链与门面计数 — 文档卫生门禁（通用）
globs:
  - "**/*.md"
  - docs/**/*
  - README*
  - "**/ROADMAP*"
  - "**/archive/**"
alwaysApply: false
---

# 执行规范 · 文档卫生（doc-hygiene）

**用这个**：ROADMAP/archive/CHANGELOG 分工 · doc 自洽 · 断链 · README 计数。**不是那个**：写作原则与结构 → **docs.mdc** · 交付走查 → **delivery** · 可发布手册 → **user-manual** `/manual`。

跨项目 CHANGELOG 审计：**文档卫生**为高频重复劳动 — ROADMAP 膨胀 · archive 混放 · doc 锚点漂移 · 门面 skills/rules 计数滞后。

## 职责三联（真源）

| 内容 | 放哪里 | 禁止 |
|------|--------|------|
| 未完成计划、候选能力 | `docs/` ROADMAP（或等价） | 大量 `done` 行长期冒充待办 |
| 已交付长叙事 / 里程碑 | `{archive_dir}/{domain}/` 时间戳文件 | 根目录 flat 堆积 · 与 ROADMAP 双份维护同一故事 |
| 用户可见版本差异 | `CHANGELOG.md` | 在 ROADMAP 写已发布细节 |

Sprint 闭合：**archive 一篇摘要**（**至少一级域子目录**，SSOT → **plan** `reference/growth-layout.md`）· plan **删 Active 正文** · CHANGELOG `[Unreleased]` — 见 **run** skill。

## doc-coherence 三联

改规格/导航/术语/公开能力时同步：

1. **正文** — README / `docs/` / OpenAPI 示例
2. **锚点** — 故事线节标题 · `#anchor` · E2E/说明书选器（见 **i18n-copy**）
3. **verify** — `verify_doc_*` 或项目等价；新增前读 **verify.mdc** §复用门禁

母版：`bash .cursor/bin/verify-doc-super-cursor.sh`（被 `verify-super-cursor.sh` 聚合）。

## 门面计数（母版 / Super Cursor）

| 变更 | 同 Sprint 同步 |
|------|----------------|
| 新增/改名 skill | 根 `README.md` · `.cursor/AGENTS.md` · `docs/training/skills.md` · `docs/migration-catalog.md` 计数 |
| 新增 rule | `verify-super-cursor.sh` · `docs/rules-catalog.md` · `AGENTS.md` execution 行 |
| 用户可见能力 | CHANGELOG `[Unreleased]` 与 README 摘要一致 |

验收：`bash .cursor/bin/cursor-coherence.sh` · `verify-doc-super-cursor.sh`。

## 禁止

- 第三个「对齐 doc/ROADMAP/README」hotfix 而不 `/plan` 文档收敛 TASK（见 **bugfix** §Follow-up）
- 只改 CHANGELOG 不更新仍对外引用的 doc 锚点
- 新建 `verify_doc_*` 而不 grep 已有脚本 / `scripts/lib/`

## 规划与验收

| 阶段 | 动作 |
|------|------|
| **/plan** | 大文档拆分 · ROADMAP 清 done · 全站 doc 收敛 → 单独 TASK；验收列写 `verify_doc_*` |
| **task-verify** | 项目 doc 脚本 / 母版 `verify-doc-super-cursor.sh` |
| **/delivery** §3 | 规格与实现 · doc-coherence |
| **/learn** | 重复模式 → `changelog-insights.md` §重复工作模式 |

## 交叉引用

- 写法与 API 契约 → **docs.mdc**
- 文案/i18n 锚点 → **i18n-copy.mdc**
- verify 脚本复用 → **verify.mdc**
- 项目路径与 doc 索引 → `.cursorGrowth/learn/dev-conventions.md`
