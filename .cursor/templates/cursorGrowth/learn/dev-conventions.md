# Dev conventions（项目特化）

> 由 **`/learn`** 维护。测试/verify 命令、分支策略、目录约定。

## Verify / test

| 场景 | 命令 |
|------|------|
| （待填） | 例：`./scripts/test.sh` · `./scripts/verify.sh` |

## 分支与提交

| 项 | 本项目 |
|----|--------|
| 主分支 | （待填） |
| commit 风格 | （待填，例 Conventional Commits） |

## 目录约定

| 路径 | 用途 |
|------|------|
| （待填） | |

## Deploy（/learn 可填 · 见 ops-deploy skill）

| 项 | 本项目 |
|----|--------|
| compose 入口 | （待填，例 `docker-compose.yml`） |
| env 模板 | （待填，例 `.env.example` → `.env`） |
| nginx 示例 | （待填，或「无」） |
| 默认 HTTP 端口 | （待填） |
| 部署 verify | （待填，例 `scripts/verify.sh` 含 L0 deploy 段） |

字段说明 → **ops-deploy** `reference/growth-deploy-fields.md`

## Skill 元数据（母版）

母版 `SKILL.md` 的 `description`（≤85 字）与 `disable-model-invocation` 分桶 → **`.cursor/docs/training/skills.md`** §disable-model-invocation 策略。

## 建议约定（/learn 可填）

> 来自 **learn** skill「建议约定」：有证据才写；默认本文件或 `rules/local`，**勿**擅自改母版 `.cursor/`。

| 证据（版本/archive） | 建议条文 | 状态 |
|----------------------|----------|------|
| （待填） | | 待确认 / 已采纳 / 已废弃 |

