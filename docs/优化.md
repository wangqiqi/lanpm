# 聊天页 CPU / 内存优化备忘

> **来源**：2026-07-31 代码评审快照（根因分析保留作历史参考）  
> **最后对齐**：2026-08-01 · chat-perf 系列 **v1.83.0–v1.87.0** 已交付（见 §6 · §11）  
> **现象**：进入聊天相关能力后 CPU / 内存占用偏高（消息较多时更明显）— 主因项已闭合，见 §1「状态」列  
> **范围**：`ChatView` · `MessageBubble` · 插件菜单/Slot · 成员侧栏 · 已读标记 · 代码高亮 · 消息缓存  
> **关联版本**：消息右键菜单 v1.78.8+ · 消息上下文 v2 v1.79.0+ · chat-perf v1.83–87  
> **文档性质**：性能债 **backlog SSOT** + 评审记录（**非** `01`/`04`/`06` 规范 SSOT；排期见 [06_ROADMAP](./06_ROADMAP.md) · `plan.md`）  
> **观测 / 预算**：`docs/specs/009-chat-perf-observe/spec.md` · `docs/templates/chat-perf-regression.md`

---

## 1. 问题摘要

### 1.1 CPU

| 严重度 | 问题 | 根因概要 | 主要位置 | 状态 |
|--------|------|----------|----------|------|
| **High** | 每条消息独立拉插件右键菜单 | `usePluginMenus` 在气泡内挂载 → N 次 `plugin.listMenus()` + N 个事件监听 | `MessageBubble.tsx` · `usePluginMenus.ts` | ✅ v1.83 |
| **High** | 每条消息挂插件 context 槽 | `PluginZoneHost` → `useSlotPlugins` → N 次 `listSlotPlugins` | `MessageBubble.tsx` · `PluginSlot.tsx` | ✅ v1.83 |
| **High** | 列表全量重渲染 | `ChatView` 内联回调 + `MessageBubble` 无 `memo`，任意 state 变更重绘全部气泡 | `ChatView.tsx` · `MessageBubble.tsx` | ✅ v1.83 |
| **Medium** | 成员侧栏定时刷新 | 侧栏打开时每 3s `loadMembers` | `MemberList.tsx` | ✅ v1.84 |
| **Medium** | 已读标记扫全表 | `useMarkRead` 依赖整份 `messages`，新消息频繁触发 effect | `useMarkRead.ts` | ✅ v1.84 |
| **Medium** | 代码块高亮成本 | `highlight.js`；无语言时 `highlightAuto` 更贵；折叠前已全量高亮 | `CodeBlock.tsx` · `highlightSetup.ts` | ✅ v1.84–86 |
| **Medium** | DM 侧栏连带重绘 | `DmSessionBar` 订阅整表 `messagesByGroup` | `DmSessionBar.tsx` | ✅ v1.84 |
| **Medium** | 消息入库全量排序 | `mergeMessage` / `upsertMessage` 每次 `sortMessages` O(n log n) | `chatStore.ts` | ✅ v1.84 |
| **Medium** | 插件菜单 IPC 冗余 | `listMenus()` 返回全部 location，客户端再 filter | `usePluginMenus.ts` | ✅ v1.85 |
| **Medium** | 插件启用惊群 | `PLUGIN_ENABLED_CHANGED` 触发 N 次 reload | `usePluginMenus.ts` · `PluginSlot.tsx` | ✅ v1.85 |
| **Low** | 置顶条扫全量消息 | `PinnedMessagesBar` 每次 render `new Map(messages)` | `PinnedMessagesBar.tsx` | ✅ v1.83 |
| **Low** | 气泡内 Router 钩子 | 每条 `MessageBubble`：`useNavigate` + `useParams` + `useLocateTask` | `MessageBubble.tsx` | ✅ v1.85 |
| **Low** | 顶栏网络轮询 | 每 8s 静默刷新网络状态 | `TopBar.tsx` | ✅ v1.92.0 `topbar-network-idle` |
| **Low** | Worker 代码高亮 | 主线程 `highlight.js` | `highlightSetup.ts` · `highlight.worker.ts` | ✅ v1.93.0 [016](./specs/016-highlight-worker/spec.md) |
| **场景** | 语音 / 会议 / 发现配对 | 各自 `setInterval` 或媒体采集，仅对应功能开启时 | 见 §4 | — |

### 1.2 内存

| 严重度 | 问题 | 根因概要 | 主要位置 | 状态 |
|--------|------|----------|----------|------|
| **High** | 消息列表全量挂载 DOM | 无虚拟列表；N 条消息 = N 棵 React 子树 | `ChatView.tsx` | ✅ v1.83 |
| **High** | 插件钩子 per-bubble | 每条气泡独立 state / listener / 闭包，随 N 线性增长 | `MessageBubble.tsx` | ✅ v1.83 |
| **Medium** | 群消息常驻内存 | `messagesByGroup` 随 `loadOlder` 累积，切群不自动裁剪 | `chatStore.ts` | ✅ v1.84 |
| **Medium** | 大数组向下传递 | 每条 `MessageBubble` 接收完整 `members` + `tasks` 引用 | `ChatView.tsx` · `MessageBubble.tsx` | ✅ v1.85 |
| **Medium** | Markdown / 高亮结果常驻 | 可见区外气泡仍挂 MD/高亮 HTML | `ChatMessageText.tsx` · `CodeBlock.tsx` | ✅ v1.86 |
| **Low** | 通知去重 Set 无界 | `notifiedIds` 模块级 `Set`，长跑可能持续增长 | `useChatNotifications.ts` | ✅ v1.85 |
| **Medium** | Markdown 解析常驻 | `ReactMarkdown` 每条实例化；无 `memo` | `ChatMessageText.tsx` · `MarkdownView.tsx` | ✅ v1.84–85 |
| **Medium** | 折叠代码仍全量高亮 | 折叠前已生成完整高亮 HTML | `CodeBlock.tsx` | ✅ v1.84 |
| **Medium** | DM 预加载占内存 | `DmSessionBar` 对全部 DM `loadMessages` | `DmSessionBar.tsx` · `chatStore.ts` | ✅ v1.84 |
| **Low** | 图片/文件预览 | 消息内图片无 `loading="lazy"` / 视口外卸载 | `MessageBubble.tsx` | ✅ v1.86 |

**与消息上下文 v2**：`resolveReplyQuote` 在 `map` 内逐条计算 → ✅ v1.85 预计算 Map；i18n 修复**不是**性能主因。

---

## 2. 问题详述

> **历史评审快照**（2026-07-31）：下列各节描述**立项前**根因与影响，便于对照 §1 状态列。已闭合项见 §11；**开放**：无（Worker 高亮 → v1.93.0 [016](./specs/016-highlight-worker/spec.md)）。

### 2.1 每条气泡调用 `usePluginMenus`（High）— ✅ v1.83

`MessageBubble` 内：

```ts
const pluginContextMenuItems = usePluginMenus('chat.message.context')
```

`usePluginMenus` 在 **每个气泡实例** mount 时：

1. 调用 `getLanpmApi().plugin.listMenus()`
2. 注册 `PLUGIN_ENABLED_CHANGED_EVENT` 监听

**影响**：消息条数为 N 时，约 **N 次 IPC + N 个 listener**。历史消息多的群进入聊天页即尖峰。

**引入时间线**：v1.78.8 `menus[]` POC（`chat.message.context`）。

---

### 2.2 每条气泡挂载 `PluginZoneHost`（High）— ✅ v1.83— ✅ v1.83

己方 / 对方气泡各有一处：

```tsx
<PluginZoneHost zone="context" context={messageContext} />
```

链路：`PluginZoneHost` → `PluginSlotHost` → `useSlotPlugins('chat.message.action')` → `listSlotPlugins`。

**影响**：N 条消息 ≈ **N 次** slot 查询（与 §2.1 叠加）。插件槽位应在 **会话级** 拉一次并下发，不应按消息实例化。

---

### 2.3 `ChatView` 全量重渲染（High）— ✅ v1.83— ✅ v1.83

- 消息 `map` 内大量 **inline 回调**（`onLinkFileToTask` 等），每次父组件 render 引用都变。
- `MessageBubble` **未** `React.memo`。
- 列表内还有 `pinnedIds.includes` / `selectedMsgIds.includes`（O(消息数×选中/置顶数)）。
- `resolveReplyQuote` 在 `map` 内对每条带 `replyToMsgId` 的消息执行（父组件任一 state 变即重算）。

**影响**：打字、多选、引用回复、置顶条交互时 CPU 持续偏高，消息越多越明显。

---

### 2.4 成员侧栏 3 秒轮询（Medium）— ✅ v1.84— ✅ v1.84

`MemberList`：`PRESENCE_POLL_MS = 3_000`，侧栏打开时 `setInterval(onRefresh)` → `loadMembers`。

单独看不致命，与 §2.1–2.3 叠加会放大体感。

---

### 2.5 `useMarkRead` 依赖整份 messages（Medium）— ✅ v1.84— ✅ v1.84

`useMarkRead(groupId, visibleMessages, …)` 在 `messages` 引用/内容变化时重新 filter 全量并 `markRead`。

实时收消息场景下 effect 触发频繁。

---

### 2.6 代码块 `highlight.js`（Medium）— ✅ v1.84–86— ✅ v1.84–86

`CodeBlock` 用 `useMemo(() => highlightCode(...))`；父组件频繁重渲染或 `highlightAuto` 路径时，长代码块成本高。

**补充**：折叠 UI（>12 行）仅隐藏溢出，**高亮在 mount 时已完成**；折叠态仍保留完整 `dangerouslySetInnerHTML` 字符串，占内存。

---

### 2.7 `DmSessionBar` 订阅全量 `messagesByGroup`（Medium）— ✅ v1.84— ✅ v1.84

```ts
const messagesByGroup = useChatStore((s) => s.messagesByGroup)
```

任意群 `upsertMessage` 都会换新 `messagesByGroup` 引用 → **DM 会话条整组件重渲染**，即使当前在看别的群。

同时 `useEffect` 对 `sessionGroupIds` 内每个 DM 调用 `loadMessages(id)`，会把多路会话消息 **预载入内存**（见 §2.8）。

---

### 2.8 `chatStore` 每次 upsert 全量排序（Medium）— ✅ v1.84— ✅ v1.84

```ts
function mergeMessage(...) {
  ...
  return sortMessages([...list, message])  // 或 patch 后仍 sortMessages
}
```

实时收消息、delivery 状态更新、撤回 patch 都会触发 **O(n log n)** 排序 + 新数组分配。大群高频更新时主线程与 GC 压力明显。

**目标态**：lamport 单调追加走尾部 insert；仅乱序/编辑走局部修正。

---

### 2.9 `usePluginMenus` 拉全量 + 惊群（Medium）— ✅ v1.85— ✅ v1.85

单次 `listMenus()` 返回所有 `location` 的菜单项，再在客户端 `filter(location)`。N 条气泡 = **N 次相同 IPC  payload**。

`PLUGIN_ENABLED_CHANGED_EVENT` 时，每个 hook 实例各自 `loadMenus()` → **惊群**（IPC + setState × N）。

**目标态**：主进程按 `location` 查询或 Renderer 单例缓存 + 单次 reload 广播给订阅者。

---

### 2.10 置顶条与文本解析（Low–Medium）— ✅ v1.83–85— ✅ v1.83–85

`PinnedMessagesBar`：有置顶时每次父组件 render 执行 `new Map(messages.map(...))`，复杂度 O(消息总数)，应复用 `ChatView` 已有 `messageById`。

`ChatMessageText`：`splitMentionSegments` / `splitTaskRefSegments` / `shouldRenderChatMarkdown` **无 memo**，父级全量 reconcile 时重复切分；含 Markdown 段则 mount `ReactMarkdown`。

---

### 2.11 气泡内 Router / 定位钩子（Low）— ✅ v1.85— ✅ v1.85

每条 `MessageBubble` 独立：

- `useNavigate()` · `useParams()` · `useLocateTask(gid)`

Router 订阅与 `useCallback` 闭包随 N 增长。应提升到 `ChatView` 或 context，气泡只收稳定函数引用。

---

### 2.12 媒体与附件（Low）— ✅ v1.86— ✅ v1.86

文件/截图消息若内联 `<img>`，默认 eager 解码；长列表滚动时 **解码与 GPU 纹理** 随历史条数累积。视口外应占位或 `loading="lazy"` + 卸载 `src`。

---

## 3. 验证方法（不改代码可自测）

| 步骤 | 操作 | 若成立则说明 |
|------|------|----------------|
| 1 | DevTools → Performance，进大群录 10s | Scripting 尖峰在 `listMenus` / `listSlotPlugins` / React reconcile → §2.1–2.3 |
| 2 | 关闭成员侧栏再对比 | CPU 明显下降 → §2.4 有贡献 |
| 3 | 小群 vs 大群 | 仅大群热 → N×气泡钩子为主因 |
| 4 | 只打字、不收消息 | 仍热 → §2.3；仅收消息时热 → §2.5 |
| 5 | 开 DM 面板、切大群对比 | DM 条随他群消息闪烁/CPU → §2.7 |
| 6 | Performance：收 100 条连续消息 | `sortMessages` / `mergeMessage` 栈帧突出 → §2.8 |
| 7 | Memory：含长代码折叠消息 | 折叠后 Heap 仍含大段 HTML 字符串 → §2.6 / §2.10 |

---

## 4. 其他场景（Low / 按功能开启）

| 场景 | 位置 | 说明 |
|------|------|------|
| 顶栏网络 | `TopBar.tsx` | 约 8s 轮询 `refreshNetwork` |
| 语音输入模式 | `ChatVoiceMediaPanel` | 切 `inputMode === 'voice'` 时加载 toolbar 插件 |
| 会议录制 | `useMeetingRecording.ts` | 录制中 1s `setInterval` 计时 |
| 会议 Mesh | `useMeetingMesh.ts` | 轮询 |
| 任务到期提醒 | `useDueTaskNotifications.ts` | 定时 scan |
| 发现配对 | `DiscoverPairingPanel.tsx` | 1s tick |
| 主进程 P2P | `RealNetworkTransport` / `udpDiscovery` | 心跳与发现（Electron 主进程） |
| 开发态 | `npm run dev` + HMR | 与业务逻辑无关，但体感也会偏热 |

排查时需先确认用户说的「开启」指上表哪一项。

---

## 5. 优化建议（按优先级）

### P0 — 结构性（预期收益最大）

1. **插件菜单单例化** — ✅ **v1.83.0**  
   - 在 `ChatView`（或聊天 Provider）调用 **一次** `usePluginMenus('chat.message.context')`。  
   - 通过 props / context 传给 `MessageBubble`，禁止每条气泡独立 hook。

2. **消息 context 插件槽会话级加载** — ✅ **v1.83.0**  
   - `chat.message.action` 的 `listSlotPlugins` 提升到 `ChatView` 一层。  
   - 气泡无 per-message `PluginZoneHost`。

3. **列表渲染减负** — ✅ **v1.83.0**（`replyQuote` Map → **v1.85.0**）  
   - `MessageBubble` 包 `React.memo` + 稳定回调（`useCallback` / 抽子组件）。  
   - `replyQuote` 预计算 `Map<msgId, ResolvedReplyQuote>`。  
   - `pinnedIds` / `selectedMsgIds` 改为 `Set` 做 O(1) 查找。

### P1 — 中等收益

4. **成员在线** — ✅ **v1.84.0**  
   - 侧栏折叠时 **停止** interval；可见性门控。

5. **已读标记** — ✅ **v1.84.0**  
   - 增量 `markRead` + debounce。

6. **代码高亮** — ✅ **v1.84–86**  
   - 折叠延后高亮 · 视口 defer · LRU · 禁止 `highlightAuto` 扫全库。

### P2 — 体验与观测

7. **性能预算**（✅ **v1.87.0** — 见 `docs/specs/009-chat-perf-observe/spec.md` §Budget）  
   - **CPU**：首屏 3s Scripting < 800ms；10s 滚动 Scripting < 500ms（packaged build）。  
   - **内存**：DOM 亚线性于历史条数；Heap 滚动/切群无阶梯暴涨。  
   - **静态守卫**：`verify:chat-perf-observe` + `verify:chat-perf*` 禁止 `MessageBubble` 内 `usePluginMenus` / `useSlotPlugins`。

8. **文档** — ⬜ 部分  
   - `插件开发.md` 可补充：`chat.message.context` 由 Host 聚合加载（非 per-bubble 生命周期）。

### P0′ — 内存专项（与 CPU 并列）

9. **虚拟列表（虚拟滚动）** — ✅ **v1.83.0**  
   - `@tanstack/react-virtual`；与按日分组 · scroll anchor 兼容。

10. **消息内存窗口（Renderer 侧）** — ✅ **v1.84.0**  
    - 2000 条/群软上限 · `downgradeInactiveGroups` · `evictGroup` 路径保留。

11. **缩小子组件 props 面** — ✅ **v1.85.0**  
    - `memberById` 快照 · 任务子集传递。

12. **富文本 / 代码懒处理** — ✅ **v1.84–86**  
    - 视口 defer · LRU 高亮 · `plaintext` 兜底。

13. **右键菜单按需挂载** — ✅ **v1.83.0**  
    - 单例 ContextMenu + `activeMsgId`。

14. **模块级缓存去界** — ✅ **v1.85.0**  
    - `BoundedSet` / `LruMap` · `notifiedIds` · `scrollMemory` 有界。

15. **Store 写入减负** — ✅ **v1.91.0**（`dm-preview-ipc` · `listDmPreviews` IPC）  
    - 追加 O(1) · DM 行读 `dmPreviewStore` · 末条消息轻量 IPC。

16. **插件 IPC 去重** — ✅ **v1.85.0**  
    - `listMenus(location?)` · 单点 invalidate。

17. **文本/Markdown 缓存** — ✅ **v1.84–93**（Worker → v1.93.0 [016](./specs/016-highlight-worker/spec.md)）  
    - `memo` · `msgId+textHash` 缓存 · 折叠延后高亮。

18. **媒体懒加载** — ✅ **v1.86.0**  
    - `loading="lazy"` · defer 态不设 `src`。

19. **Zustand 选型** — ✅ **v1.90.0**（`chat-perf-store-hooks` · `chatStoreActions`）

### P3 — 快速收益（改动小，可穿插）

| 项 | 做法 | 状态 |
|----|------|------|
| 置顶条 | 传入 `messageById: Map` | ✅ v1.83 |
| `selectedMsgIds` | `useState<Set<string>>` | ✅ v1.83 |
| `formatTime` | 模块级纯函数 + memo | ✅ |
| `PinnedMessagesBar` | `memo` + 稳定回调 | ✅ v1.83 |
| 开发态 | 生产包对比 `npm run dev` | 见 §9.1 · spec §Budget |
| 顶栏网络 | 可见性/空闲门控轮询 | ✅ v1.92.0 `useNetworkIdlePoll` |
| Worker 高亮 | 后台线程 `highlight.js` | ✅ v1.93.0 `highlight.worker.ts` · [016](./specs/016-highlight-worker/spec.md) |

---

## 6. 建议实施顺序（Sprint 候选）

| 序 | 任务 | 预估 | 验收（CPU / 内存） |
|----|------|------|-------------------|
| 1 | 抽出 `ChatPluginMenusProvider`，移除气泡内 `usePluginMenus` | 小 | ✅ **v1.83.0** — IPC `listMenus` 仅 1 次；气泡 mount 无插件 hook |
| 2 | 会话级 slot 缓存 + 移除/按需 `PluginZoneHost` | 中 | ✅ **v1.83.0** — `listSlotPlugins` 与 N 解耦 |
| 3 | `MessageBubble` memo + 稳定 props + 单例 ContextMenu | 中 | ✅ **v1.83.0** — 打字时无全列表 reconcile；DOM 中 Dropdown 数量不随 N |
| 4 | **虚拟列表** | 中 | ✅ **v1.83.0** — `@tanstack/react-virtual`；与按日分组兼容 |
| 5 | 成员轮询 / 已读增量 | 小 | ✅ **v1.84.0** — 侧栏关无 interval；收消息 CPU 平滑 |
| 6 | 高亮/Markdown 懒加载 + LRU 缓存 | 小 | ✅ **v1.84.0** — 折叠长代码延后高亮；`ChatMessageText` memo |
| 7 | `messagesByGroup` 窗口 + 切群降级 | 中 | ✅ **v1.84.0** — 2000 条/群 + `downgradeInactiveGroups` |
| 8 | `verify:chat-perf` 防回归 | 小 | ✅ **v1.83.0** — `npm run verify:chat-perf` |
| 9 | `upsertMessage` 增量排序 + DM 轻量订阅 | 小 | ✅ **v1.84.0** — 追加 O(1)；DM 行级 selector |
| 10 | 插件 IPC `location` 过滤 + 单点 invalidate | 小 | ✅ **v1.85.0** — `listMenus(location?)` payload 与 N 解耦 |
| 11 | 代码折叠延后高亮 + Markdown memo | 小 | ✅ **v1.84.0** — 长代码折叠 Heap 下降 |
| 12 | 图片 lazy + DM 预加载策略 | 小 | ✅ **v1.84.0** DM 仅当前会话；✅ **v1.86.0** MD `img` lazy + 附件 defer 门控 |

**可并行**：序 9、10 与序 1–2 同属「减 IPC / 减 store 写」；序 11–12 可在虚拟列表前先做。

**建议 Sprint 名**：`chat-perf`（与 `api-v0.4` 等功能 Sprint 分开）。

### 6.1 预期收益（定性）

| 优化簇 | CPU | 内存 | 体感 |
|--------|-----|------|------|
| 插件单例 + IPC 去重（序 1–2、10） | ★★★★ | ★★★ | 进大群首屏 |
| memo + 单例菜单（序 3） | ★★★★ | ★★ | 打字/多选 |
| 虚拟列表（序 4） | ★★★ | ★★★★ | 千条历史滚动 |
| Store 增量（序 9） | ★★★ | ★★ | 活跃群收消息 |
| 懒高亮/Markdown（序 6、11） | ★★ | ★★★ | 代码/MD 多的群 |
| 内存窗口（序 7） | ★ | ★★★★ | 多群切换长跑 |

---

## 7. 非目标（本次评审不涵盖）

- Convex / 后端（本仓为 Electron 客户端）
- 消息上下文 v2 业务逻辑正确性（引用/转发/置顶功能本身）
- 发版流程与 CHANGELOG
- 主进程 P2P 协议层重写（仅列观测点，见 §4）

---

## 8. 修订记录

| 日期 | 说明 |
|------|------|
| 2026-07-31 | 初稿：聊天页 CPU 评审结论与优化建议 |
| 2026-07-31 | 增补：内存问题表 · IM 行业对照 · 维护约定 · 虚拟列表与缓存策略 |
| 2026-07-31 | 二轮：DmSessionBar/store 排序/IPC 惊群/Markdown·代码懒处理/P3 快赢 · §6 扩至 12 项 |
| 2026-08-01 | v1.88.0 docs-optim-closeout：§1 状态列 · §2 历史横幅 · §5 开放项 · ROADMAP 同步 |

---

## 9. 行业对照：QQ / 微信 / 同类 IM 怎么做

> 以下为公开架构模式与客户端常见实践归纳，**非**腾讯/微信官方实现细节。

| 维度 | QQ / 微信 / Telegram 等常见做法 | LanPM 现状（v1.86） | 剩余 / 目标 |
|------|--------------------------------|---------------------|-------------|
| **消息列表** | 虚拟列表 / Cell 复用；上万条可滑 | ✅ `@tanstack/react-virtual`（v1.83） | — |
| **长按/右键菜单** | 菜单定义注册 **一次**；交互时带 `messageId` 弹出 | ✅ `ChatPluginMenusProvider` + 单例 ContextMenu（v1.83） | — |
| **扩展能力** | 工具栏、「+」、独立页；**不**每条消息挂插件容器 | ✅ 会话级 slot 缓存；气泡无 `PluginZoneHost`（v1.83） | — |
| **列表更新** | 单条 insert/patch/delete，避免整表重绘 | ✅ `MessageBubble` memo + 稳定 props（v1.83） | ✅ store-hooks v1.90.0 |
| **在线状态** | 长连接推送 delta；面板不可见时停刷 | ✅ 侧栏关无 interval；可见性门控（v1.84） | 推送 delta（远期） |
| **已读** | 按 seq/msgId **增量**上报，批量 debounce | ✅ 增量已读 + debounce（v1.84） | — |
| **引用预览** | 存 preview 字段或会话级缓存 Map | ✅ `replyQuote` 预计算 Map（v1.85） | — |
| **富文本/代码** | 懒加载、缩略、Worker/后台高亮 | ✅ 视口 defer + LRU/折叠 + Worker 高亮（v1.84–93） | — |
| **内存** | 历史在本地 DB，内存只保留窗口 | ✅ 2000 条/群窗口 + 切群降级（v1.84） | — |
| **DM 列表** | 会话列表只显示 preview，不预载全历史 | ✅ `listDmPreviews` IPC + `dmPreviewStore`（v1.91） | — |
| **排序写入** | 按 seq 追加 O(1)，乱序才重排 | ✅ 追加 O(1) 路径（v1.84） | — |
| **插件 IPC** | 按场景注册一次，启用变更单点刷新 | ✅ `listMenus(location?)` + 单点 invalidate（v1.85） | — |

**可抄的三条原则**（写进 Code Review 自检）：

1. **配置一次，交互带 id** — 菜单、插件、权限不在列表 item 里重复初始化。  
2. **看得见才算账** — 虚拟列表 + 视口内才做重活（高亮、Markdown、大图）。  
3. **增量同步** — 已读、在线、新消息都尽量 delta，避免 O(N) 扫全表。  
4. **写入便宜** — 追加消息不重排全表；订阅 store 时粒度到 `groupId`，不订整表。

### 9.1 Electron 客户端额外注意

| 项 | 说明 |
|----|------|
| **主进程 vs 渲染进程** | 聊天热点在 Renderer；Performance 录的是渲染线程，P2P 心跳在主进程另看任务管理器 |
| **生产包基准** | `npm run dev` + HMR 会抬高 CPU；性能验收用 **packaged build** 或 `npm run build` 后启动 |
| **DevTools 开销** | 开着 Memory/Performance 面板本身占内存；抽检完关闭 |
| **多窗口** | 若未来多窗口共享 store，需避免每窗重复 `listMenus`（单例应进程级） |

---

## 10. 文档维护与立项约定

### 10.1 本文档放什么

| 放这里 | 不放这里 |
|--------|----------|
| 现象、根因、`file` 线索、对标、验证步骤 | 已闭合 Sprint 长叙事 → 本地 `.cursorGrowth/archive/`（勿在 docs 引用具体文件） |
| **未做**的优化项与优先级 | 产品需求正文 → `01` |
| 性能预算、Review 自检清单 | 排期承诺 → `06_ROADMAP` |

### 10.2 生命周期

```text
Performance 评审 / 用户反馈
    → 写入 优化.md §1 / §2
    → 立项：06_ROADMAP 加「聊天性能」条目（链到 §6）
    → plan.md 拆 TASK（如 TASK-chat-perf-*）
    → 实现 + verify:chat-perf + Performance 抽检
    → CHANGELOG ### Fixed/Changed
    → 优化.md 对应行标 ✅ vX.Y.Z（或迁入 §11 已解决）
```

### 10.3 PR / 功能自检（聊天相关）

新增或改动聊天 UI 时，作者自检：

- [ ] 未在 `MessageBubble`（或列表 item）内新增 `usePluginMenus` / `useSlotPlugins` / 每 item IPC  
- [ ] 未在列表 item 内新增 `useNavigate` / `useParams`（路由钩子应在上层）  
- [ ] 列表 item 已 `memo`，回调 `useCallback`，避免 inline 箭头传入  
- [ ] 无随消息条数 N 线性增长的 listener / interval / DOM 包装（如每行一个 `Dropdown`）  
- [ ] 重计算（高亮、Markdown、引用解析）有缓存或仅在视口内执行  
- [ ] 长跑结构（Set/Map）有上限或按群清理  
- [ ] Store 更新避免不必要的全表 `sort` / 全表新数组（追加走 O(1) 路径）  
- [ ] 订阅 Zustand 时粒度到当前 `groupId`，避免 `messagesByGroup` 整表订阅  

### 10.4 观测与回归

**SSOT**：性能预算与 packaged 抽检步骤见 `docs/specs/009-chat-perf-observe/spec.md` §Budget · §10.4；走查勾选表见 `docs/templates/chat-perf-regression.md`；实测记录见 `docs/chat-perf-baseline.md`。

| 时机 | 动作 |
|------|------|
| Sprint 收尾 | 大群（≥500 条）Performance 录 10s，对比 §Budget 与 `chat-perf-baseline.md` |
| 发版前（聊天有大改） | DevTools Memory 快照：进大群 → 滚动 30s → 切群，看 Detached DOM / Listener |
| CI | `verify:docs-optim-closeout` + `verify:chat-perf-observe` + `verify:chat-perf*`（静态）+ 既有 `verify:message-context-v2` |

### 10.5 与 ROADMAP 衔接

聊天性能主 Sprint 已交付 **v1.83.0–v1.93.0**。`06_ROADMAP` §1 记剩余债。

| 模块 | 功能 | 备注 |
|------|------|------|
| **聊天性能** | Worker 高亮 [016](./specs/016-highlight-worker/spec.md) | v1.93.0 · 主序列全闭合 |

---

## 11. 已解决（归档区）

| 版本 | 项 | 说明 |
|------|-----|------|
| **v1.88.0** | 文档 hygiene（docs-optim-closeout） | §1 状态列 · §2 历史说明 · §5 开放项 · ROADMAP 同步 · `verify:docs-optim-closeout` |
| **v1.87.0** | 性能观测闭环（chat-perf-observe） | §5.7 预算 SSOT · §10.4 链 spec · `verify:chat-perf-observe` · 回归模板 · 基线表 |
| **v1.86.0** | 视口 defer 重内容（chat-perf-viewport） | §5.12 视口外不进 MD/高亮 · §5.18 `loading="lazy"` · `UserAvatar` defer |
| **v1.85.0** | chat-perf-p3 | §6 序 10 · Router 上提 · LRU/有界缓存 · props 瘦身 · `replyQuote` Map |
| **v1.84.0** | 序 5–7、9、11–12（chat-perf-follow） | store 增量 · 内存窗口 · 已读/轮询 · DM 轻量 · 延后高亮 |
| **v1.83.0** | 序 1–4、8（chat-perf Sprint） | `ChatPluginMenusProvider` · slot 缓存 · `MessageBubble` memo · 单例右键菜单 · 虚拟列表 · `verify:chat-perf` |
| — | （更早暂无） | 优化落地后从 §1 移入并注明 tag |
