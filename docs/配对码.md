# 短配对码 · 离线局域网连接方案

> 状态：**已批准执行** → `.cursorGrowth/plan.md` **SPRINT-PAIR-01**（`PLAN_APPROVED:true` · `TASK-PAIR-01`…`11` · 目标 **1.42.0**）  
> 背景：跨网段、无外网、无摄像头场景下，「复制 IP」「二维码」均不合适；需 **键盘输入 6 位数字** 完成首次配对。  
> 关联：`docs/02_技术实现建议.md` §13.6 · `docs/03_数据模型与协议草案.md` §6.2 · `docs/飞鸽飞秋.md` §6.1 A5

---

## 1. 要解决的问题

| 现状 | 用户痛点 |
|------|----------|
| 同网段 UDP 自动发现（`43123`） | ✅ 零配置；**主路径，继续保留** |
| 跨网段手动 `IP:43124` | 能连，但要记 IP、找隐藏菜单 |
| 复制地址 | 离线两台桌面机之间 **无法传递剪贴板** |
| 二维码 | 很多机器 **没有摄像头**（服务器、工控机、台式机） |

**目标**：在 **不依赖互联网、不依赖摄像头** 的前提下，让用户用 **6 位数字 + 键盘** 打通 **群组发现目录**（不是「连同事」）；可见群后 **申请加入** 或持 **群邀请码** 入群；已连接节点之间 **中继共享** 发现视野（`hop`≤2）。

---

## 2. 设计原则

| 原则 | 说明 |
|------|------|
| **数据不出域** | 不配公网配对服务；全程 UDP/TCP 局域网 |
| **无中心服务器** | 配对状态仅在「发起方本机」内存 + 短时广播；不做账号云 |
| **先同网段、后跨网段** | 同网段：只输入码；跨网段：允许加「可选尾码 / 路由探测」 |
| **以群为中心** | 产品叙事围绕 **可发现群组**；连接码打通目录，**不自动加群** |
| **入群治理** | **申请 + 管理员审批**；或 **群邀请码**（输入后直接成为成员） |
| **发现中继** | TCP 对端互推 `discover_relay`（peers/groups/seeds），`hop` 上限 **2** |
| **短码可读** | 6 位十进制，分组显示 `847 293`，避免 0/O、1/l 歧义（纯数字） |

### 非目标（本期不做）

- 公网中继、短信验证码、扫码
- 替代群组 `autoDiscover` 语义（连接码只打通 **发现目录**）
- 发现可见即自动进群（须 **审批** 或 **邀请码**）
- 强制替换现有 `host:port` 手动输入（保留高级入口）

---

## 3. 用户故事

### 3.1 同一交换机 / 同一 WiFi（同网段，无外网）

```
同事 A：发现 → 「分享群组连接码」→ 屏幕显示码 + **本机可发现群名** +（跨网段）IP
同事 B：发现 → 「查找群组」→ 输入码 → **群组 Tab** 出现远程群 → **申请加入** 或输入 **群邀请码** 直接入群
```

无需 IP、摄像头、互联网。

### 3.2 跨网段（如 192.168.30.x ↔ 192.168.20.x，路由可达）

```
A（20.109）显示：配对码 847 293 · 地址 192.168.20.109
B（30.170）：
  方案甲（推荐）：输入 847293 → 客户端按本机路由表对「可达网段」发配对查询（见 §5.3）
  方案乙（兜底）：输入 847293 + 选择「我知道对方网段」→ 填 20.109（IP 仅 4 段数字，比记端口简单）
```

配对成功后写入种子，**下次开机自动连**，不再输入。

### 3.3 机房 / 无屏服务器

- A 通过 SSH 执行 `lanpm pairing start` 打印码（**v1.44** ✅）
- B 桌面端输入码 + 服务器 IP（尾段），或 `lanpm pairing join <code> --cross-subnet --host <ip>`
- 或 U 盘导出/导入 `lanpm-peer.json`（**v1.44** ✅，见 §8）

---

## 4. 交互设计（草案）

### 4.1 入口合并

**发现** 面板顶部改为两个主按钮（取代散落的「手动添加节点」「种子」）：

| 按钮 | 角色 | 行为 |
|------|------|------|
| **分享群组连接码** | 发起方 | 展示码 + 可发现群列表；UDP/TCP 配对 |
| **查找群组** | 加入方 | 6 位码（跨网段 + IP/尾段） |

高级：展开「手动输入 IP:端口」「管理种子」「导出/导入配对文件」。

### 4.2 发起方界面

```
┌─────────────────────────────────────┐
│  让同事加入我                        │
│                                     │
│         8 4 7   2 9 3               │  ← 大号等宽字体
│                                     │
│  5:00 后失效 · 本机 192.168.20.109   │  ← 小字，跨网段时参考
│                                     │
│  [ 复制配对信息 ]  [ 取消 ]          │  ← 复制给有 IM 的场景，非主路径
└─────────────────────────────────────┘
```

- 配对进行中：UDP `pairing_offer` + TCP 监听 `pairing_resolve`
- 有人连上：Toast「某某 已连接」+ 可选「信任此设备」

### 4.3 加入方界面

```
┌─────────────────────────────────────┐
│  加入同事                            │
│                                     │
│  配对码  [ 8 ][ 4 ][ 7 ][ 2 ][ 9 ][ 3 ] │
│                                     │
│  ☐ 对方和我不在同一 WiFi（跨网段）    │  ← 勾选后显示可选 IP
│     对方 IP  [ 192.168.20.109    ]   │
│                                     │
│            [ 连接 ]                  │
└─────────────────────────────────────┘
```

- 默认同网段：只填码  
- 跨网段：勾选 + IP（或仅填 IP **最后一段** `109`，客户端补全网段候选，见 §5.3）

### 4.4 与现有能力关系

| 现有 | 配对码之后 |
|------|------------|
| UDP 自动发现 | 保留；同网段仍优先自动 |
| 手动 `IP:43124` | 收进「高级」 |
| 发现种子 | 配对成功 **自动 add seed** |
| TCP 握手带群组（1.41.4+） | 配对连上后走同一套 `registerTcpPeer` |

---

## 5. 技术方案（无中心）

### 5.1 配对会话（发起方）

发起「让同事加入我」时：

1. 生成 `pairingId`（uuid）、`code`（6 位数字，`100000–999999` 或 `000000–999999` 补零显示）
2. `expiresAt = now + 5min`（可配置）
3. 内存表：`activePairing: { code, pairingId, expiresAt, attempts }`
4. 启动定时器：每 **2s** 发送 `pairing_offer`（UDP），直到过期或取消
5. TCP `43124` 接受 `pairing_resolve`（明文 wire，在 DH 握手之前）

**安全**：短码 + 短时效 + 发起方可选「需确认」；不替代群组密钥与消息加密。

### 5.2 UDP 报文（扩展 discovery 通道，`43123`）

与现有 `{ v:1, kind:'discovery', payload }` 并列：

```typescript
// 发起方周期广播
interface PairingOfferPacket {
  v: 1
  kind: 'pairing_offer'
  code: string              // "847293"
  pairingId: string
  deviceId: string
  userId: string
  displayName: string
  listenPort: number        // 默认 43124
  host?: string             // 源 IP
  expiresAt: string         // ISO8601
}

// 加入方查询（广播或单播）
interface PairingLookupPacket {
  v: 1
  kind: 'pairing_lookup'
  code: string
  joinerDeviceId: string
  joinerDisplayName: string
}

// 发起方单播回复（仅当 code 匹配且未过期）
interface PairingFoundPacket {
  v: 1
  kind: 'pairing_found'
  pairingId: string
  deviceId: string
  userId: string
  displayName: string
  listenPort: number
  host: string
}
```

**同网段流程**：B 输入码 → B 广播 `pairing_lookup` → A 回复 `pairing_found` → B `connectManualHost(host, port)` → 现有 TCP 握手。

### 5.3 跨网段：配对查询怎么找到 A？

UDP 广播 **不过路由器**，仅同网段有效。跨网段采用 **分层策略**（由易到难，可分期）：

| 阶段 | 策略 | 说明 |
|------|------|------|
| **P0** | 用户勾选跨网段 + 输入 **完整 IP** 或尾段（高级） | 码用于 **校验连对机器**（防连错） |
| **P1** | **路由表引导单播** ✅（v1.43）：`route print` / `ip route` → 各子网 `.255` 并行 `pairing_lookup` | 跨网段 **只填码** |
| **P2** | 可选「网段扫描」✅（v1.46 · SPRINT-PAIR-04）：对可达 `/24` 分批 `pairing_lookup` `.1–.254` | 慢、默认关；高级模式 |

**推荐产品默认**：P0（码 + 可选 IP）即可解决当前 **30.x ↔ 20.x** 场景——用户只需看 A 屏幕上的 `192.168.20.109`，比记 `IP:43124` 少记端口；码保证没连错人。

**IP 尾段简化（可选 UX）**：

- A 显示 `109` 为「地址尾码」
- B 勾选跨网段，只输入 `847293` 和 `109`
- B 客户端根据本机已知种子 / 历史 / 路由表构造候选：`192.168.20.109`、`192.168.30.109`… 逐个 `pairing_lookup` 单播

### 5.4 TCP `pairing_resolve`（兜底）

UDP 被防火墙拦时，加入方对 **已知 IP** 的 `43124` 发送 wire 消息（与 `handshake` 同级，未加密）：

```typescript
| { kind: 'pairing_resolve'; code: string; joinerDeviceId: string; joinerDisplayName: string }
| { kind: 'pairing_resolve_ok'; ...WirePeerProfile }
| { kind: 'pairing_resolve_fail'; reason: 'expired' | 'mismatch' | 'rate_limit' }
```

成功后走现有 `connectManualHost` / 握手逻辑。

### 5.5 配对成功后的持久化

1. `addDiscoverSeed(`${host}:${listenPort}`)` — 已有 API  
2. `connectManualPeer` 立即执行  
3. （可选）`sync_meta` 记录最近配对设备昵称，发现页「最近协作设备」

### 5.6 发现中继 `discover_relay`（SPRINT-PAIR-01 · TASK-PAIR-09）

已 TCP 连接的节点周期性互推本机合并后的发现目录（明文，无业务密文）：

```typescript
interface DiscoverRelayPacket {
  v: 1
  kind: 'discover_relay'
  hop: number              // 发送方剩余跳数，初始 ≤2，每转发 -1
  viaDeviceId: string      // 直接来源设备
  peers: { deviceId; userId; displayName; host; listenPort }[]
  groups: DiscoverableGroupAdvert[]
  seeds?: string[]         // host:port，接收方合并并尝试 TCP
}
```

| 规则 | 说明 |
|------|------|
| 触发 | `peer_advert` 后 / 周期（如 30s）向所有 TCP 对端发送 |
| 合并 | 写入 `discoverGroupRegistry`、peer 列表、种子 |
| 跨网段 | 20 连 30 一台 → 继承 30 网段 UDP 视野内各 `autoDiscover` 群 |
| 横向同步 | 20 网段 Y 与 X 已有 TCP → Y 同步 X 的中继目录 |
| 安全 | 目录可见 ≠ 能进群；`hop` 为 0 不再转发 |

### 5.7 入群治理（TASK-PAIR-10 / 11）

| 路径 | 行为 |
|------|------|
| **申请加入** | 发现群点「申请加入」→ 群主/管理员审批 → 通过后成为成员 |
| **群邀请码** | 管理员生成 6 位码（绑定 `groupId`、TTL、一次性）→ 输入后 **直接入群** |

---

## 6. 安全与滥用

| 风险 | 对策 |
|------|------|
| 6 位码被猜中 | 5 分钟 TTL；每分钟限流；失败 5 次锁定该码；可选发起方点选确认 |
| 局域网嗅探 | 配对只暴露 **可达性 + 身份**；业务仍走 DH + AES（现有） |
| 重放 | `pairingId` 一次性；`pairing_found` 后作废 |
| 骚扰连接 | 发起方可随时「取消配对」；关闭配对窗口即停播 |

**信任模型**：与飞鸽一致——**首次局域网信任**；企业若要零信任，靠后续「需确认」开关。

---

## 7. 实现分期（与 plan 对齐）

| 版本 | 范围 | 验收 |
|------|------|------|
| **1.42**（SPRINT-PAIR-01） | 连接码 · 跨网段 IP/尾段 · `discover_relay`（hop≤2）· 入群申请/审批 · 群邀请码 · `verify:pairing-code` | 30↔20 中继见全段群；邀请码直入群 |
| **1.43+** | 路由表引导单播（跨网段只填码） | SPRINT-PAIR-02 |
| **1.44**（SPRINT-PAIR-03） | U 盘 `lanpm-peer.json` · CLI `lanpm pairing` | 无屏 SSH + 文件摆渡 |
| **1.49**（DISCOVER-TEST-02） | VirtualLan 2 节点 · `verify:discover-sim` | 内存 UDP 总线 |
| **1.50**（DISCOVER-TEST-03） | VirtualLan 全矩阵 · `verify:discover-sim-full` | 中继 · 跨子网 · 种子 SQLite · 治理用例 |
| **1.51**（DISCOVER-TEST-04） | VirtualLan 故障注入 · TCP-only 兜底 sim | `udpDropRate` / `udpDelayMs` · 限流 sim |
| **后续** | Docker nightly · Renderer E2E | 可选 CI |

### TASK 清单（plan.md）

| ID | 内容 |
|----|------|
| TASK-PAIR-01 | UDP 配对 + 状态机 |
| TASK-PAIR-02 | TCP `pairing_resolve` |
| TASK-PAIR-03 | IPC pairing |
| TASK-PAIR-04 | 发现页群组中心 UI |
| TASK-PAIR-05 | 跨网段 IP/尾段 |
| TASK-PAIR-06 | 种子 + peer_advert |
| TASK-PAIR-07 | 配对安全 TTL/限流 |
| TASK-PAIR-08 | verify + 文档 |
| TASK-PAIR-09 | `discover_relay` |
| TASK-PAIR-10 | 入群申请 + 审批 |
| TASK-PAIR-11 | 群邀请码 |

**执行顺序**：`01→02→03→09→06→10→11→04→05→07→08`

### 自动化测试矩阵（VirtualLan · v1.50+）

| 场景 | 脚本 / 模块 | 断言 |
|------|-------------|------|
| 同子网 UDP 发现 + 配对码 | `verify:discover-sim` | 广播发现 · offer→lookup→found→TCP |
| 3 节点 `discover_relay` | `discover-sim-full` / `three-node-relay` | C 经 B 见 A 群 · hop≤2 |
| 跨子网路由引导 `.255` | `cross-subnet-route-guided` | `10.0.1.255` + `10.0.2.255` lookup |
| 尾段 `109` 展开 | `cross-subnet-tail` | `buildPairingHostCandidates` |
| `/24` subnetScan | `subnet-scan` | 分批 lookup 命中 host |
| 种子写入 SQLite | `db-scenarios` / electron-node | `discover_seeds_json` meta |
| 群邀请码 UDP | `group-invite` | `group_invite_found` |
| 配对文件 TCP | `peer-file` | loopback TCP + 虚拟 IP 元数据 |
| 入群申请仓储 | `db-scenarios` / `join-request` | `group_join_requests` pending |
| UDP 全丢包 | `udp-blackhole` | lookup 超时 |
| TCP-only 兜底 | `tcp-only-fallback` | `disableUdp` + `pairing_resolve` |
| 配对限流 | `pairing-rate-limit` | 超额 lookup → `rate_limit` |
| 发现弹窗 UI（分享/查找/组网帮助） | `verify:e2e-discover` | Playwright Electron · `data-testid` |

---

## 8. 与「U 盘配对文件」并列（✅ v1.44 · SPRINT-PAIR-03）

完全无屏 / 空气隔离场景：

```json
{
  "v": 1,
  "host": "192.168.20.109",
  "port": 43124,
  "deviceId": "...",
  "displayName": "ubuntu-lanpm",
  "fingerprint": "sha256(...)"
}
```

B 端「导入配对文件」→ 等同种子 + 首次 TCP。与短码 **互补**，不互斥。

---

## 9. 开放问题（已拍板 · 2026-07-30）

| # | 问题 | 决定 |
|---|------|------|
| 1 | 码长度 | **6 位** |
| 2 | 跨网段展示 | 码 + **IP**（同网段仅码） |
| 3 | 发现后加群 | **申请审批** 或 **群邀请码**；邀请码输入 **直接入群** |
| 4 | 发现中继 `hop` | **上限 2** |
| 5 | 匿名群 | 连接只打通目录，与群类型无关 |
| 6 | 多网卡 | `pairing_offer` 各 LAN 接口各发一份 |

---

## 10. 和「完全没有局域网」的边界

短配对码 **不能** 替代物理组网：

- 两台机器必须先 **同一二层/三层可达**（交换机、热点、路由）。  
- **没有网线的两台孤机** → 请先接交换机或开热点，再谈配对码。

产品 Setup 可增加一步图示（**v1.45 · SPRINT-SETUP-NET ✅**）：

> 「请把要协作的电脑接到 **同一个路由器或交换机**（不需要上网）」

发现页「组网帮助？」可 **随时回看图示**（**v1.47 · SPRINT-DISCOVER-HELP ✅**），与 Setup 共用 `NetworkPrereqContent`。

---

## 11. 小结

| 问题 | 答案 |
|------|------|
| 离线、无摄像头能连吗？ | **能**——同网段只输入码；跨网段码 + IP/尾段 |
| 20 连 30 一人能见全 30 群吗？ | **能**（经 `discover_relay`，hop≤2） |
| 进群要审批吗？ | **要**（或管理员 **群邀请码** 直入） |
| 下一步 | **`/run`** 执行 SPRINT-PAIR-01 |

---

*文档版本：2026-07-30 · SPRINT-PAIR-01 已批准*
