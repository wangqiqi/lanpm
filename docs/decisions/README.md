# 决策摘要（纳入 Git）

**用这个**：仍生效的产品/技术拍板、性能预算、验收锚点 — 短、可检索、可审查。  
**不是那个**：Sprint 长叙事、全文 spec、评审快照 → `.cursorGrowth/archive/`（本地，gitignore）。

| 放这里 | 放 Growth archive |
|--------|-------------------|
| 结论 · 边界 · 预算阈值 · verify 命令 | 过程笔记 · 完整 spec · `优化.md` 全文 |

命名：`docs/decisions/<topic>.md`
