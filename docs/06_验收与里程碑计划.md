# 06_验收与里程碑计划

> 验收范围、里程碑排期与 **RC 发布门禁**。索引见 [00_文档导航.md](./00_文档导航.md)。  
> 手验步骤见 [05_测试与联调发布.md](./05_测试与联调发布.md)。

**当前版本**：`1.0.7`（与 `package.json` 一致；真机手验延期见 §2.6）。

---

## 1. 第一阶段功能清单（MVP）

### ✅ P0（必须完成）

| 模块   | 功能                               |
| ---- | -------------------------------- |
| 首次配置 | 用户名 + 设备名 + 部门 + 后缀自动生成/手动修改     |
| 主框架  | 顶部栏 + 底部导航 + 主内容区                |
| 聊天   | 成员列表 + 消息收发 + 已读回执 + 桌面通知        |
| 代码分享 | 语法高亮（自动识别语言）                     |
| 看板   | 4列 + 拖拽移动 + 增删改查                 |
| 任务树  | 父子层级 + 展开折叠 + 进度汇总               |
| 甘特图  | 时间轴 + 进度条 + 拖拽调整                 |
| 文件   | 上传/下载 + LibreOffice 本地预览 + 网址内嵌 WebView |
| 群组   | 项目/职能/匿名 三类群组                    |
| 驾驶舱  | 项目总览 + 报表生成 + API-Key 配置         |
| 网络   | UDP 广播发现 + P2P 消息/文件传输 + AES 加密  |

> **联调**：M2–M5 可用 `NetworkStub`；真网见 [05](./05_测试与联调发布.md) §6。

### ✅ P1（第二阶段）

| 模块      | 功能        |
| ------- | --------- |
| 屏幕共享    | 桌面/应用窗口共享 |
| 语音通话    | 1v1 语音    |
| 思维导图    | 独立编辑器     |
| 插件系统    | 加载第三方扩展   |
| 移动端 Web | PWA 基础版本  |

### ⏳ P2（未来规划）

| 模块       | 功能            |
| -------- | ------------- |
| 视频通话     | 多人视频会议        |
| 完整插件市场   | 插件发布/安装/更新    |
| 原生移动 App | iOS + Android |

---

## 1.1 P0 / P1 功能边界（补充）

| 能力 | 阶段 | 说明 |
|------|------|------|
| 私聊、@提及、已读回执、`/task` 建任务 | P0 | 聊天主链路 |
| 表情、书签浏览器导入导出 | P1 | 体验增强 |
| 全局搜索（任务+消息，成员可 P1） | P0 任务+消息 / P1 成员 | 顶栏搜索至少覆盖两类 |
| 甘特依赖 FS/SS/FF/SF、里程碑 | P0 | 与 01 §9 一致 |
| 甘特导出 PNG/PDF | P1 | 与 01 §9 导出能力一致 |
| 文件断点续传、传输队列（≤3） | P0 | 与 01 §10.4 一致 |
| AI 任务审核/评估/周报 | P0 手动触发 | 自动化巡检归 P1 |
| 在线/离开/离线三态 | P0 | 成员列表 🟢🟡⚪ |

---

## 2. RC 发布门禁（原 M7 验收清单）

### 2.1 自动化（CI / 本地）

| 项 | 命令 | 状态 |
|----|------|------|
| 全量回归 | `npm run verify:m7` | ✅ |
| 视觉令牌守卫 | `npm run verify:visual` | ✅（V-14a，已纳入 verify:m7） |
| 项目健康 | `npm run verify:project` | ✅（lint + 版本 + search） |
| 全局搜索 | `npm run verify:search` | ✅（已纳入 verify:m7） |
| 生产构建 | `npm run build` | ✅ |
| §16 模块存在性 | `npm run verify:m7-acceptance` | ✅ |
| SQLite 崩溃恢复 | `npm run verify:m7-stability` | ✅ |
| 性能冒烟 | `npm run verify:m7-perf` | ✅ |
| M0–M6 里程碑 | 见 `verify:m7` 串联 | ✅ |

- `npm run ensure:native`：检测 better-sqlite3 是否能在 Electron 下加载，必要时自动 `@electron/rebuild`
- `verify:m7` / 含 SQLite 的 `verify:*`：通过 `scripts/run-electron-node.mjs` 使用 Electron 内置 Node

### 2.2 §16 功能验收映射

| 章节 | 要点 | 自动化 | 手验 |
|------|------|--------|------|
| 16.1 首次配置 | 向导、后缀、唯一 ID | SetupWizard 存在 | 启动新 user-data-dir |
| 16.2 主界面 | 顶栏/底栏/主题/i18n | verify:m1 | `npm run dev` |
| 16.3 聊天 | 文本/代码/@/私聊/已读 | verify:m2 | [05](./05_测试与联调发布.md) §3 |
| 16.4 看板 | 四列拖拽、OTHER 原因 | verify:m3 | [05](./05_测试与联调发布.md) §4 |
| 16.5 任务树 | 父子、进度聚合 | verify:m3 | [05](./05_测试与联调发布.md) §4 |
| 16.6 甘特 | 日/周/月、依赖、导出 P1 | verify:m4 | 拖拽 + 导出 |
| 16.7 文件 | 列表/预览/书签 P1 | verify:m4 | LibreOffice 预览 |
| 16.8 群组 | 三类群、匿名限制 | verify:m5 | 匿名群无历史 |
| 16.9 驾驶舱 | 指标/报表/API Key | verify:m5 | /cockpit |
| 16.10 分布式 | UDP 发现、加密 P2P | verify:m6 | [05](./05_测试与联调发布.md) §6 |
| 16.11 非功能 | 冷启动/内存/P95 | verify:m7-perf 部分 | [05](./05_测试与联调发布.md) §5 |
| 16.12 联调 | Stub + 真网 | verify:m2 + m6 | 双 `--user-data-dir` |

### 2.3 已知限制（RC，非 P0 阻断）

> **SSOT**：PRD 目标栈与 RC 现状差异；**非** `1.0.0` P2 阻断。v1.1 候选见 **§6**。

| 项 | PRD 目标 | RC 现状 | 跟踪 ID |
|----|----------|---------|---------|
| 任务多机同步 | CRDT（Yjs） | **已落地（SPRINT-TASK-CRDT → v1.1.0）**：`task_crdt` + 双写 `task_patch` + state-vector 离线补拉；SQLite 仍为查询源 | TASK-156–163 |
| 聊天离线补拉 | 7 天窗口 | `chat_sync_request` / `chat_sync_batch` 分页续传（>100）已落地 | ARCH-07 |
| 聊天历史 UI | 可读全量历史 | 初始 200 + `loadOlderMessages` 分页已落地 | TASK-133 |
| 文件传输 | 断点续传 | **P2P pull 续传已落地（SPRINT-FILE-RESUME → v1.2.0）**：`fromOffset` + `{fileId}.partial` + SQLite download 进度；本地假分片上传续传仍为既有 PRD-F-10 | DOC-F-05 / TASK-164–169 |
| 看板标签 | 独立「标签」字段 | **已落地（SPRINT-BOARD-TAGS → v1.3.0）**：`tags: string[]` + `tags_json`；看板/详情可编辑；与 `priority` 解耦；经 `task_patch`/`task_crdt` 薄同步 | DOC-F-02 / TASK-171–175 |
| 标签筛选/色板 | 看板 OR 筛选 + 本机色 | **已落地（SPRINT-BOARD-TAG-FILTER → 目标 v1.5.0）**：工具栏多选筛选；hash/覆盖色；不新增 sync | TASK-183–188 |
| 群标签字典 | 跨端一致标签色 | **已落地（SPRINT-GROUP-TAG-DICT → 目标 v1.6.0）**：`group_tag_meta` + `group_tag_patch` LWW；色板/芯片读群字典；本机色可一次性导入 | TASK-189–194 |
| 协同焦点 Presence | Yjs Awareness | **已落地（SPRINT-TASK-AWARENESS → 目标 v1.4.0）**：`task_awareness` + 看板/树焦点指示；非文本 caret；不落库 | TASK-177–182 |
| 描述框文本 caret | Awareness caret | **已落地（SPRINT-TEXT-CARET-AWARENESS → 目标 v1.7.0）**：`caret: { field: description, offset }`；详情/看板弹窗彩色竖线+名 | TASK-195–200 |
| 远端文件 P2P | 对端拉取闭环 | `fileSyncService` + FilesView「从局域网下载」 | — |
| WebRTC | DataChannel | **v1.0 维持 TCP+UDP**（见 [01](./01_产品需求文档.md) §1.3.1） | v1.1 可选 |
| 群组发现 | 中心化目录 | UDP 广播 `groups[]` + 本机缓存 ~60s；加入为本地建群副本 | — |
| UDP 发现 | — | 广播 + 组播 `239.255.43.123:43123`（ARCH-03） | — |
| 协议空转 | — | `UNIMPLEMENTED_SYNC_TYPES` 已空；`task_crdt` / `member_event` 均可 publish | TASK-146 · TASK-157 |
| 匿名群会话 | 可持久化历史（可选） | **内存会话**：进程退出即失；无文件 Tab、不入 SQLite | M5 / §16.8 |
| 已读回执离线补拉 | 离线补齐已读 | `read_receipt_sync_request` / `_batch` 已落地（7 天窗，TASK-151–154） | — |
| 发布失败可恢复 | outbox / 重试 | 聊天 `failed`+自动/手动重试；task/file toast；无独立 outbox 队列 | TASK-141/142 |

### 2.4 §16.11 性能手测记录

| 指标 | 目标 | 实测 | 环境 |
|------|------|------|------|
| 冷启动 | ≤ 3s | _延期：无真机手测环境（2026-07-09）_ | — |
| 空闲内存 | ≤ 200MB | _延期：同上_ | — |
| 聊天内存 | ≤ 350MB | _延期：同上_ | — |
| Tab 切换 P95 | ≤ 100ms | _延期：同上_ | — |
| SQLite 初始化 | — | **32.4ms**（schema+100 写） | `verify:m7-perf` 2026-07-09 |
| 加解密 P95 | — | **0.02ms** / 帧 | `verify:m7-perf` 2026-07-09 |
| 路由解析 P95 | — | **<0.001ms** | `verify:m7-perf` 2026-07-09 |

> 自动化：`npm run verify:m7-perf`（下三行已填，纳入 `verify:release-gate`）。冷启动/内存/Tab P95 需生产包真机（方法 [05](./05_测试与联调发布.md) §5）；无设备前标延期，不阻断 RC。

### 2.5 M7 发布前回归

- [x] `verify:m7` 全绿
- [x] Linux 自动化回归：`npm run verify:m7`（2026-05-29；本机 2026-07-09 `verify:release-gate`）
- [x] 三平台自动化冒烟：CI `verify.yml` 矩阵 `ubuntu-latest` / `macos-latest` / `windows-latest` → `verify:m7`（DOC-ACC-02 自动化层；真机 UI 肉眼见 §2.6 延期）
- [x] 亮/暗七页截图基线生成：`verify:visual-screenshots`（AUTO-20，2026-05-29）
- [x] 亮/暗主题视觉抽检（静态 `verify:visual` HOV/SEM；甘特像素以 `npm run dev` 为准，见 [05](./05_测试与联调发布.md) §2.2）
- [x] 匿名群：无文件 Tab、退出无历史（M5-02）
- [x] AI：无 Key 时本地报告降级（M5-06）
- [x] Office 预览：本地 soffice，无外链

#### DOC-02 收尾清单（2026-05-29 · 2026-07-09 自动化闭合）

| 类别 | 项 | 状态 |
|------|-----|------|
| 自动化 | `verify:m7` / `verify:project` / `verify:docs-code --strict` | [x] |
| 自动化 | `verify:visual`（含 SEM 语义色、HOV 令牌、VIS-07b 字号） | [x] |
| 自动化 | `verify:visual-screenshots`（AUTO-20） | [x] |
| 自动化 | `verify:profile-panel` / `verify:i18n-en` | [x] |
| 自动化 | CI 三平台 `verify:m7`（`verify.yml`） | [x] DOC-ACC-02 |
| 自动化 | `verify:dual-stub`（发现/聊天/已读/任务） | [x] DOC-ACC-03 代理 |
| 自动化 | `verify:m7-perf` | [x] §2.4 下三行 |
| 人工 | 亮/暗七页截图对照 [05](./05_测试与联调发布.md) §2.2 | [x] 静态门禁 |
| 人工 | Win/mac/Linux **真机 UI** 肉眼 | 延期 → §2.6 |
| 人工 | **真网**双机、冷启动/内存手测、英文折行肉眼 | 延期 → §2.6 |
| post-RC | Yjs / 断点续传 / 看板标签 | 见本文 **§6** |

### 2.6 手验待办（SSOT）

> 手验 SSOT 即本表。发版自动化：`npm run verify:release-gate`。  
> **2026-07-09**：无真机/双机环境时，以自动化层闭合验收；下表「延期」项已在正式 `1.0.0` 发布时**显式接受风险**（建议后续补真机）。

| ID | 任务 | 状态 |
|----|------|------|
| DOC-ACC-02 | 三平台 UI | [x] **自动化**：CI `verify.yml` 三 OS × `verify:m7`（2026-07-09）；**延期**：真机七页肉眼 |
| DOC-ACC-03 | 双机联调（发现 → 加群/私聊；已读） | [x] **自动化**：`verify:dual-stub`（本机 2026-07-09）；**延期**：真网双机 |
| — | 性能（冷启动/内存/Tab P95） | [x] **自动化**：`verify:m7-perf` 填 §2.4 下三行；**延期**：冷启动/内存/Tab 手测 |
| DOC-ACC-04 | I18N 英文长文案折行 | [x] **自动化**：`verify:i18n-en`（无 CJK）；**延期**：全页折行肉眼 |
| — | 发布 **1.0.0** | [x] **2026-07-09** 版本门面 `1.0.0`（`SPRINT-1.0-GATE`）；git tag `v1.0.0` → `/release`；真机延期风险已接受 |

**维护（非阻断）**：圆角遵守 [04](./04_交互与UI约定.md) §1.3；post-RC 见 **§6**。IPC / i18n / UI / 单测门禁见 [05](./05_测试与联调发布.md) §1。

---

## 3. 验收标准（§16 条文）

### 16.1 首次配置

- 首次启动弹出配置向导；必须填写用户名和设备名
- 用户名重复时自动添加 `-yymm` 后缀；允许手动修改
- 配置后生成唯一 ID 并保存

### 16.2 主界面

- 顶栏：项目下拉、驾驶舱、搜索、主题、语言、个人信息
- 底部 5 导航：聊天、看板、任务树、甘特图、文件
- 亮色/暗色主题；全局搜索至少覆盖任务与消息（P0）

### 16.3 聊天

- 成员列表含在线/离开/离线；文本、代码块、@、私聊、`/task`
- 已读按 `userId` 聚合；@ 触发桌面通知
- M2：NetworkStub 双实例；M6：真网双机复验

### 16.4 看板

- 四列 `todo`/`doing`/`done`/`other`；`@dnd-kit/core` 跨列拖拽（`docs/04` §8）
- OTHER 须填原因；**M3**：`BoardView` + `verify:m3`；手验 [05](./05_测试与联调发布.md) §4

### 16.5 任务树

- 父子展开折叠；父进度为子任务均值；与看板同源 `taskStore`
- **M3**：`TaskTreeView`；见 [05](./05_测试与联调发布.md) §7

### 16.6–16.12

甘特、文件、群组、驾驶舱、分布式、非功能、联调条文与 **§1 P0 清单** 及 **§2.2 映射表** 一致；非功能指标：冷启动 ≤3s、空闲 ≤200MB、聊天 ≤350MB、交互 P95 ≤100ms。

---

## 4. 里程碑计划

| 阶段     | 名称       | 内容                        | 预估时间    |
| ------ | -------- | ------------------------- | ------- |
| M0     | 项目初始化    | Electron + 首次配置 + **NetworkStub** | 2天      |
| M1     | 主框架      | 顶部栏 + 底部导航 + 主题切换         | 2天      |
| M2     | 聊天模块     | 成员列表 + 消息收发 + 代码高亮 + 已读回执（**Stub 联调**） | 3天      |
| M3     | 看板 + 任务树 | Kanban + 树形任务管理           | 3天      |
| M4     | 甘特图 + 文件 | 甘特图 + 文件预览/传输             | 3天      |
| M5     | 群组 + 驾驶舱 | 三类群组 + 驾驶舱 + API-Key      | 2天      |
| M6     | 网络层      | **真实网络替换 Stub** + UDP/P2P/加密 | 3天      |
| M7     | 测试与优化    | 集成测试 + 性能优化 + Bug 修复      | 2天      |
| **合计** |          |                           | **20天** |

---

## 5. 文档迁移说明

原 `docs/03_验收与里程碑计划.md` + `docs/08_M7_RC验收清单.md` 已合并为本文件；原 `docs/07_M3_*` 实现摘要见 `docs/05` §7。  
原 `docs/08_post-RC规划与已知限制.md`（2026-05-29）已并入本文 **§2.3**（已知限制）与 **§6**（v1.1 候选）；文档体系恢复为 **01–06** 六篇正文。

---

## 6. v1.1 / post-RC 候选（非 P2 阻断）

- ~~Yjs 任务 CRDT 与现有 `task_patch` 迁移策略（DOC-TECH-02）~~ → **已交付** SPRINT-TASK-CRDT（双写共存 + 离线 SV；打版 `/release` → **v1.1.0**）
- ~~文件断点续传与传输队列 UX 统一（DOC-F-05）~~ → **P2P pull 续传已交付** SPRINT-FILE-RESUME（`verify:file-resume`；**v1.2.0**）；本地假分片上传续传为既有能力
- ~~看板独立标签字段（产品定稿后改 [03](./03_数据模型与协议草案.md) + [04](./04_交互与UI约定.md)）（DOC-F-02）~~ → **已交付** SPRINT-BOARD-TAGS（打版 `/release` → **v1.3.0**）；筛选/色板见 SPRINT-BOARD-TAG-FILTER
- WebRTC DataChannel（可选，与真网联调计划绑定）
- 群组发现：跨子网目录、已加入群状态同步、匿名群发现策略
- ~~`task_crdt` Awareness / 协同光标 UI~~ → **焦点 Presence 已交付** SPRINT-TASK-AWARENESS（`/release` → **v1.4.0**）；**描述框文本 caret 已交付** SPRINT-TEXT-CARET-AWARENESS（`/release` → **v1.7.0**）
- ~~看板标签色板 / 筛选~~ → **已交付** SPRINT-BOARD-TAG-FILTER（OR 筛选 + 本机色板；`/release` → **v1.5.0**）
- ~~群级标签色字典同步~~ → **已交付** SPRINT-GROUP-TAG-DICT（`group_tag_meta` / `group_tag_patch`；`/release` → **v1.6.0**）
