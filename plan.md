# LanPM 完整执行计划（v2.0 对齐版）

> 依据文档：`docs/00` ~ `docs/06`（见 [docs/00_文档导航.md](./docs/00_文档导航.md)）
>
> 目标周期：20 天（M0-M7）

---

## 1. 项目目标与边界

### 1.1 核心目标
- 交付一个无中心服务器的分布式局域网/VPN 协作客户端（Electron）。
- 在 P0 阶段完成 IM + 项目管理 + 文件共享 + 驾驶舱 + 网络基础能力闭环。
- 建立可演进的工程结构，支持 P1/P2 功能连续扩展。

### 1.2 本期范围（P0）
- 首次配置、主框架、聊天、代码分享、看板、任务树、甘特图、文件、群组、驾驶舱、网络层。
- 跨平台目标：Windows 10+ / macOS 12+ / Ubuntu 22.04+。

### 1.3 非目标（本期不做）
- 多人视频会议、完整插件市场、原生移动 App（P2）。
- 强依赖公网服务的中心化同步方案。

---

## 2. 交付原则

- **需求一致性**：任何实现必须能映射到 `docs/01~06` 的明确条目（索引见 `docs/00`）。
- **分层实现**：先骨架后联调，先稳定后增强，避免跨层返工。
- **验收驱动**：每个里程碑都有对应 checklist、演示路径和回归点。
- **变更可追踪**：每次阶段性调整同步更新 `CHANGELOG.md` 与本计划。

---

## 3. 工作分解结构（WBS）

### 3.1 产品与交互（对应 `docs/01`）
- IA 与导航：顶部栏 + 5 大视图 + 群组维度切换。
- 身份体系：用户名/后缀、设备标识、多设备在线聚合。
- 模块交互：聊天、看板、任务树、甘特图、文件、驾驶舱。
- 匿名群组约束：匿名代号、无历史同步、仅文本聊天。

### 3.2 技术底座（对应 `docs/02` + `docs/04`）
- 主进程网络模块：UDP 发现、连接管理、心跳广播。
- P2P 传输层：WebRTC DataChannel 消息/文件通道。
- 数据同步：聊天顺序保证 + 任务 CRDT + 文件元数据广播。
- 安全链路：DH 密钥交换 + AES-256-GCM + HMAC。
- 本地存储：SQLite（可加密）+ IndexedDB 分工。
- 数据模型：按 `docs/04` 实现 User/Group/Message/Task/File 与 SyncEnvelope。

### 3.3 交付与验证（对应 `docs/03`）
- P0 全量功能验收项落地。
- M0-M7 每阶段构建、联调、回归闭环。
- 性能与可靠性指标验证（启动、内存、响应、恢复）。

---

## 4. 20 天里程碑总览（M0-M7）

| 阶段 | 时间 | 目标 | 主要输出 | 退出标准 |
|------|------|------|----------|----------|
| M0 | Day 1-2 | 项目初始化与首次配置 | Electron 骨架、配置向导、**NetworkStub** | 可首次启动并保存身份；Stub 可双实例互通 |
| M1 | Day 3-4 | 主框架与主题能力 | 顶部栏、底部 5 视图导航、主题切换 | 可在 5 视图间稳定切换 |
| M2 | Day 5-7 | 聊天与代码分享 | 成员列表、消息收发、@提及、代码高亮、已读回执 | **Stub 双实例**消息与回执闭环 |
| M3 | Day 8-10 | 看板与任务树 | 看板 4 列拖拽、任务树层级与进度汇总 | 任务状态与层级数据一致 |
| M4 | Day 11-13 | 甘特图与文件模块 | 甘特时间轴、依赖、文件库、预览、传输队列 | 时间调整与文件传输可用 |
| M5 | Day 14-15 | 群组与驾驶舱 | 三类群组、项目总览、报表、API-Key 配置 | 驾驶舱可生成周报/月报 |
| M6 | Day 16-18 | 网络层强化 | 用真实 UDP/WebRTC **替换 Stub** | 局域网多机稳定、加密可验证 |
| M7 | Day 19-20 | 测试优化与发布准备 | 集成测试、性能优化、缺陷收敛、发布包 | 通过验收清单并可打版 |

---

## 5. 逐阶段执行计划（详细）

### 5.1 M0：初始化与身份配置
- 任务清单：
  - 初始化 `src/main` / `src/preload` / `src/renderer` 分层结构（React 18 + TypeScript）。
  - 实现首次配置向导：用户名、设备名、部门、头像。
  - 实现用户名冲突后缀规则：自动 `-yymm` + 手动修改校验。
  - 持久化本地身份档案（userId、displayName、devices）。
  - 实现 **NetworkStub**：内存/本机回环通道，模拟节点发现与消息投递（接口与 `docs/04` SyncEnvelope 对齐）。
- 交付物：
  - 向导页面 + 本地配置存储 + 启动跳转流程 + NetworkStub 模块。
- 验收点：
  - 重启后可恢复身份，重复用户名能自动分配后缀。
  - 同一机器可启动两个实例，通过 Stub 互相发现并收发测试消息。

### 5.2 M1：主框架与导航体系
- 任务清单：
  - 按 `docs/05` 实现布局骨架、主题、底部 5 Tab 路由。
  - 顶部栏：项目下拉、驾驶舱入口、全局搜索占位、主题与语言切换。
  - 底部栏：聊天/看板/任务树/甘特图/文件 5 入口；职能/匿名群隐藏不可用 Tab。
  - 主内容区域：按视图切换路由与容器状态保持。
  - 按 `docs/05` §8 集成 Ant Design 5.x + CSS Modules + Zustand（已拍板）。
- 交付物：
  - 主框架完整可交互原型（无核心业务数据亦可演示）。
- 验收点：
  - 视图切换流畅，主题切换可记忆。

### 5.3 M2：聊天主链路
- 任务清单：
  - 成员列表与在线状态渲染（🟢🟡⚪）。
  - 文本消息、@提及、桌面通知。
  - 代码块消息：语言识别与高亮渲染。
  - 已读状态机：发送中/已发送/已读。
- 交付物：
  - 聊天会话页面与消息模型（含代码消息类型）。
- 验收点：
  - 基于 NetworkStub 的双实例可完成消息与已读回执闭环（不要求真实局域网）。
  - @提及可触发桌面通知。

### 5.4 M3：任务管理（看板 + 任务树）✅

- 任务清单：
  - 看板 4 列：TODO/IN PROGRESS/DONE/OTHER。
  - 卡片增删改查、拖拽跨列、OTHER 原因填写。
  - 任务树结构展示、展开折叠、父任务进度聚合。
  - 看板与任务树共享任务主数据源。
  - 聊天 `/task` → `task_ref` → 看板闭环。
- 交付物：
  - `src/shared/task/*`、`taskRepository`、`taskService`、`ipc/task`
  - `BoardView`、`TaskTreeView`、`taskStore`；`@dnd-kit/core`
  - 文档：[docs/07_M3_看板与任务树实现说明.md](./docs/07_M3_看板与任务树实现说明.md)
- 验收点：
  - 拖拽改状态后树结构与进度同步正确。
  - `npm run verify:m3`；手验 [docs/06](./docs/06_测试与联调手册.md) §2。
- **未做（记入 M4/M6）**：任务 P2P 同步、Yjs、甘特时间轴、看板列内 Sortable、UI 删除任务。

### 5.5 M4：甘特图 + 文件（✅ v0.8.1-m4）
- 任务清单：
  - 集成 `gantt-task-react`（见 `docs/05` §6.3、`docs/01` §9）。
  - 甘特图日/周/月视图切换与任务条拖拽。
  - 依赖关系（FS/SS/FF/SF）与里程碑标识。
  - 甘特导出 PNG/PDF（M4-04）。
  - 文件列表、筛选、上传下载、传输队列管理。
  - 文档/图片/PDF/视频/文本/网址预览能力集成。
  - 书签 Netscape HTML 导入/导出（M4-08）。
- 交付物：
  - 甘特视图组件 + 文件服务组件 + 预览适配层 + 书签服务。
- 验收点：
  - 甘特调整可写回数据，文件预览链路可跑通；`npm run verify:m4`。

### 5.6 M5：群组与驾驶舱（✅ v0.9.0-m5）
- 任务清单：
  - 实现项目/职能/匿名三类群组的创建与切换。
  - 匿名群组约束策略落地（仅文本、无历史、无文件 Tab）。
  - 驾驶舱指标卡、项目进度视图、部门完成率视图。
  - 周报/月报生成与 API-Key 配置面板。
  - AI 手动评估（脱敏字段外发）。
- 交付物：
  - 群组管理（TopBar 创建/切换）+ 驾驶舱页面 + 报表导出能力。
- 验收点：
  - 群组行为符合类型规则，驾驶舱报表可产出；`npm run verify:m5`。

### 5.7 M6：网络层与加密闭环（✅ v0.10.0-m6）
- 任务清单：
  - UDP 广播发现（43123）、心跳 5s、离线 15s。
  - LAN TCP 可靠数据通道 + 指数退避重连（1s→20s）。
  - ECDH + AES-256-GCM 加密收发。
  - `initNetwork` 工厂：`LANPM_NETWORK=stub|real`（默认 real，Stub 保留）。
  - 同用户多设备在线聚合（presence + 心跳）。
- 交付物：
  - `network/real/` + `crypto/` + `verify:m6`。
- 验收点：
  - `npm run verify:m6`；双机手验见 `docs/06` §4（`LANPM_TCP_PORT` 区分端口）。

### 5.8 M7：测试优化与发布
- 任务清单：
  - 按 `docs/03` 第 16 章与 `docs/06` 逐项验收。
  - 性能指标验证：启动、内存、交互响应（测量方法见 `docs/06` §2）。
  - 故障恢复验证：离线同步、崩溃恢复、重启会话恢复。
  - 打版前缺陷清零（P0 阻断项必须归零）。
- 交付物：
  - 验收报告、性能报告、发布候选包（RC）。
- 验收点：
  - 完成 P0 全项验收，允许进入正式打版。

---

## 6. 验收策略（执行方式）

### 6.1 验收分层
- **功能验收**：按 `docs/03` 第 16 章逐条打勾。
- **联调验收**：跨模块主链路（聊天建任务->看板->树->甘特->驾驶舱）。
- **性能验收**：启动、内存、响应、传输吞吐。
- **安全验收**：加密链路与密钥轮换策略验证。

### 6.2 核心验收路径
- 路径 A：首次配置 -> 进入主界面 -> 加入群组 -> 发消息。
- 路径 B：聊天 `/task` -> 看板拖拽 -> 任务树进度汇总 -> 甘特时间调整。
- 路径 C：上传文件 -> 预览 -> 下载 -> 传输记录查询。
- 路径 D：驾驶舱查看项目 -> 生成周报/月报 -> AI 评估提示。

---

## 7. 风险清单与缓解

| 风险 | 影响 | 缓解措施 |
|------|------|----------|
| UDP 跨平台差异 | 节点发现不稳定 | 主进程做统一适配层，建立多平台回归用例 |
| P2P 建链失败率 | 消息/文件不可达 | 增加重试退避、候选通道、故障回退提示 |
| CRDT 与视图一致性 | 任务状态错乱 | 单一任务源模型，视图层订阅更新 |
| 大文件传输中断 | 用户体验差 | 分片 + 断点续传 + 队列并发控制 |
| 性能超标 | 发布阻塞 | M4 后启动持续性能基线监控 |
| 文档与实现偏差 | 返工 | 每里程碑末做“文档-实现映射审计” |

---

## 8. 协作与节奏

### 8.1 日常节奏
- 每日站会：同步昨日产出、今日目标、阻塞项。
- 每两天一次里程碑小结：范围、风险、变更决策。

### 8.2 文档维护机制
- 需求变更：先改 `docs/01` / `docs/03`，涉及字段时同步 `docs/04`，最后改 `plan.md` 与 `docs/00` 追溯表。
- 阶段完成：更新 `CHANGELOG.md`（最新在最上）。
- 归档规则：废弃文档与临时稿统一放 `archive/`，文件名按时间戳规范。

---

## 9. 当前执行状态（滚动）

### 9.1 已完成
- 完成需求文档 v2.0 正式化与拆分编号。
- 完成 `docs` 原型 HTML 归档与归档记录。
- 完成 `docs/04_数据模型与协议草案.md`（实体、枚举、报文、状态机、存储分工）。
- 完成文档自洽性修复（`docs/00` 导航、验收补全、M2/M6 排期、README 对齐）。
- 完成文档深化（`docs/05` UI 约定、`docs/06` 测试手册、SQLite 初稿、Yjs 粒度拍板）。
- 锁定前端 UI 栈：Ant Design + CSS Modules + Zustand + `gantt-task-react`；文档 v0.3.4 收口。
- 完成文档全量 Review 与一致性修复（v0.3.5，见 `archive/20260528_190000_文档全量Review报告.md`）。
- 完成 M0-A 工程骨架：`electron-vite` + React 18 + Ant Design 5 + Zustand（v0.4.0-m0）。
- 完成 M0-B 本地存储基线：`better-sqlite3` 建库建表（`docs/04` §11）+ `users`/`devices` 仓储（v0.4.1-m0）。
- 完成 M0-06 首次配置向导：强制向导 + IPC 身份持久化（v0.4.2-m0）。
- 完成 M0-07~M0-10：后缀规则（本机+LAN）、`NetworkTransport` 接口、`NetworkStub` 文件总线联调（v0.5.0-m0）。
- 完成 M1-01 / M1-03：五视图 `HashRouter`、底部 Tab、群组类型 Tab 规则（v0.5.1-m1）。
- 完成 M1-02 / M1-05 / M1-06：顶部栏、主题持久化、i18n 基线（v0.5.2-m1）。
- 完成 M2-01 文本聊天收发；配置向导设备名自动 hostname、移除原生菜单栏（v0.6.0-m2）。

### 9.2 待执行（按优先级）
- [x] 建立 `src/` 工程骨架（M0）
- [x] 实现首次配置向导 UI（M0-06，v0.4.2-m0）
- [x] 后缀规则局域网唯一性校验（M0-07，v0.5.0-m0）
- [x] NetworkStub 传输层（M0-08~10，v0.5.0-m0）
- [x] 五视图路由与群组 Tab 规则（M1-01、M1-03，v0.5.1-m1）
- [x] 顶部栏完整能力（M1-02~06：驾驶舱/搜索占位/主题/语言/用户面板，v0.5.2-m1）
- [x] 实现聊天消息链路与回执（M2，v0.7.0-m3）
- [x] 实现任务看板/任务树联动（M3，见 `docs/07`）
- [x] 实现甘特图与文件预览/传输（M4，v0.8.1-m4）
- [x] 实现群组模型与驾驶舱报表（M5，v0.9.0-m5）
- [x] 实现节点发现与加密传输（M6，v0.10.0-m6）
- [ ] 完成验收回归与 RC 打版（M7）

> 执行层拆解与实时状态见：`todo.md`
> 任务编号规范：`Mx-yy`（如 `M2-06`），完成标准与依赖关系在 `todo.md` 中维护。

---

## 10. 关键决策（已拍板）

| 主题 | 已确定方案 | 说明 |
|------|------------|------|
| 前端框架 | React 18 + TypeScript | 生态成熟、维护与招聘更稳、后续迁移与对接便利 |
| UI 组件库 | Ant Design 5.x | 企业协作密集型界面；见 `docs/05` §8 |
| 样式方案 | CSS Modules | 与 Ant Design 主题配合，不引入 Tailwind |
| 全局状态 | Zustand | 跨视图/群组状态管理 |
| 甘特图 | `gantt-task-react` | M4 集成，与任务 CRDT 同源 |
| Office 预览主链路 | 本地 LibreOffice 优先 | 核心数据不出域，保障内网数据边界 |
| AI 阶段范围 | P0 手动触发，P1 自动化 | 先满足审核可用，再逐步提升自动化能力 |
| 全局搜索 P0 | 任务 + 消息 | 成员 P1；文件在文件模块内检索 |
| 传输加密 | UDP + WebRTC + 应用层 AES-GCM/DH/HMAC | 无独立 TLS 层；见 `docs/02` §14.4 |
| 看板拖拽 | `@dnd-kit/core` | M3 看板跨列；见 `docs/05` §8 |

### 10.1 你已确认的规则（已落文档）

- 外部 API 是明确需求，可接入（前提：仅脱敏字段外发）。
- “无互联网依赖”解释为“核心业务数据不出域”，而非“设备绝对离线”。
- 已读回执按 `userId` 聚合：同用户任一设备阅读即已读。
- 冲突处理采用“最新事件优先”，问题回滚仅影响本人，不影响他人状态。
- 网络协议参数已有开发初稿，可先按默认值联调。
- M2–M5 使用 NetworkStub 联调；M6 切换真实 UDP/WebRTC（方案 A）。

---

## 11. 打版门禁（Release Gate）

- P0 验收项 100% 通过（`docs/03` 第 16 章）。
- 无 P0/P1 阻断级缺陷。
- 启动时间、内存、响应满足 `docs/02` 非功能指标。
- 跨平台最小可用验证完成（Win/macOS/Linux）。
- `CHANGELOG.md`、`plan.md`、`docs/` 状态一致后允许发版。