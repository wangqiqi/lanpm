# LanPM 设计微调备忘（全页面视觉对齐）

> **用途**：亮/暗主题下，**所有主视图 + 全局骨架 + 关键弹层** 的视觉层次、分界与令牌对齐。  
> **范围**：与 `npm run verify:visual` 七页一致，并含顶栏/底栏/群组壳层/弹窗。  
> **背景**：2026-05-29 视觉审计 + 聊天页截图反馈。  
> **规范 SSOT**：[docs/04_交互与UI约定.md](./docs/04_交互与UI约定.md) §1.3–§1.7、§6  
> **自动化**：`npm run verify:visual`（发版前须绿）· 亮暗截图 `verify:visual-screenshots`（七页）

---

## 1. 全页面范围（是否都涉及？）

**是——本备忘覆盖产品内所有「用户会长时间停留」的界面**；但**改动优先级**不同：聊天 + 全局骨架为 P0，其余页以「对齐标准 + 亮暗手验」为主，避免无问题硬改。

| 层级 | 页面 / 模块 | 路由或入口 | 样式主文件 | 备忘章节 |
|------|-------------|------------|------------|----------|
| **骨架** | 主布局 | `MainLayout` | `layout/MainLayout.module.css` | §3 S0 |
| **骨架** | 顶栏 | 全局 | `layout/TopBar.module.css` | §3 S0 |
| **骨架** | 底栏 5 Tab | 全局 | `layout/BottomNav.module.css` | §3 S0 |
| **七页①** | 首次配置 | `#/` Setup | `features/setup/SetupWizard.module.css` | §3.1 |
| **七页②** | 聊天 | `/g/:id/chat` | `features/chat/chat.module.css` | §3.2 |
| **七页③** | 看板 | `…/board` | `features/board/board.module.css` | §3.3 |
| **七页④** | 任务树 | `…/tree` | `features/tree/tree.module.css` | §3.4 |
| **七页⑤** | 甘特 | `…/gantt` | `features/gantt/gantt.module.css` | §3.5 |
| **七页⑥** | 文件 | `…/files` | `features/files/files.module.css` | §3.6 |
| **七页⑦** | 驾驶舱 | `/cockpit` | `views/CockpitView.module.css` + Ant `Card` | §3.7 |
| **壳层** | 群组视图标题 | 非聊天四视图 | `views/GroupView.tsx` + `ui/ViewHeader` | §3.8 |
| **弹层** | 发现 / 建群 / 个人设置等 | Modal | Ant + `discover.module.css` 等 | §3.9 |
| **异常** | 崩溃页 | — | `app/crash.module.css` | 已用令牌，仅回归 |

> ①～⑦ 与 `tests/static/verify-visual.ts` 中 `SEVEN_PAGE_VIEWS` 一致。

---

## 2. 跨页面对齐标准（全页共用）

实现或手验时，**每一页**对照下表（亮/暗各一遍）。

| 标准 ID | 约定 | 通过判据 | 常见违规 |
|---------|------|----------|----------|
| **A-01** | 页面底 | 主内容区背景 `--lanpm-bg`（`MainLayout`） | 某块误用 `#fff` 硬编码 |
| **A-02** | 内容面板 | 卡片/列/分栏：`--lanpm-surface-solid` + `1px --lanpm-border` + **8px** 圆角 | 面板与底同色、无边框 |
| **A-03** | 分隔线 | 区域分界：`--lanpm-border` 或 `--lanpm-bar-border`（顶/底栏 **0.5px**） | 亮色下过淡「看不见」 |
| **A-04** | 面色差优先 | 先改铺底再加强线；暗色勿叠粗黑线 | 只靠加粗边框 |
| **A-05** | 模块间距 | 视图内 `gap: 12px`；主区内边距 **16px**（聊天 `mainChat` 例外见 §3.2） | 间距 8/16 混用无规律 |
| **A-06** | 标题区 | 任务类视图：`ViewHeader` + `ViewToolbar`；聊天无重复大标题（§6.1） | 双标题、标题区无层次 |
| **A-07** | 工具条 | `ViewToolbar` / `ViewSegment` / `RegionButton` 用 `--lanpm-font-body` | 离散 `12px` 字号 |
| **A-08** | 交互态 | hover `--lanpm-hover-bg`；选中 `--lanpm-accent-fill-strong` | 未走 `regionInteract` |
| **A-09** | 语义色 | 成功/警告/危险用 `--lanpm-success` 等 | Ant 默认绿 `#52c41a` |
| **A-10** | 亮暗成对 | 改 `global.module.css` 时 light/dark 成对；改 Ant 时 `ThemeProvider` 同步 | 只改一侧 |

---

## 3. 分页面现状与问题

### 3.0 全局骨架（S0）— 影响**所有页面**

| ID | 区域 | 现状 | 问题 | 期望 |
|----|------|------|------|------|
| P-01 | 亮色 `--lanpm-border` / `--lanpm-bar-border` | 约 12% 透明度 | 亮色分界偏弱 | 仅 light 略加强，或局部加强 |
| P-02 | 顶栏 ↔ 主内容 | 底 `0.5px bar-border` + 毛玻璃 | 与同色主区对比弱 | 保持毛玻璃，必要时底缘对比↑ |
| P-08 | 顶栏按钮组 | 仅 `gap` | 操作与搜索/设置挤在一起 | 分组间距 + 可选竖分隔 |
| P-09 | 底栏 Tab | 已有 Tab 间竖线 + 选中顶条 | 基本满足 A-03 | 手验即可 |

### 3.1 首次配置 Setup

| ID | 检查项 | 现状 | 对齐 |
|----|--------|------|------|
| P-SU-01 | 全屏底 | `--lanpm-bg` | ✅ A-01 |
| P-SU-02 | 主卡片 | `--lanpm-surface-group` + 阴影/分隔 | ✅ 手验亮暗卡片与背景可分 |
| P-SU-03 | 步骤列表选中 | `accent-fill` + 左边线 | ✅ A-08 |
| P-SU-04 | 与主应用过渡 | 完成后进 MainLayout | 手验：Setup 与聊天页层次风格一致 |

### 3.2 聊天 Chat（**P0 改造重点**）

| ID | 区域 | 现状 | 问题 | 期望 |
|----|------|------|------|------|
| P-CH-01 | 成员侧栏 | `border-right` + **`--lanpm-bg`** | 与主区同色「糊」 | **`--lanpm-surface-solid`** + 保留右边线 |
| P-CH-02 | 模式条 / 上下文条 | 有 `border-bottom`，底 **`--lanpm-bg`** | 条带层次感弱 | 可选 `surface-solid` 或与 P-CH-01 统一策略 |
| P-CH-03 | 消息区 | `--lanpm-bg` | 作为「沟底」合理 | 保持；与侧栏/输入区形成对比 |
| P-CH-04 | 输入区 `.composer` | `border-top` + **`--lanpm-bg`** | 与消息区难分 | **`--lanpm-surface-solid`** |
| P-CH-05 | DM 条 `.dmBar` | `surface-solid` + border | 亮色对比偏弱 | 随 P-01 / P-CH-01 一并手验 |

### 3.3 看板 Board

| ID | 检查项 | 现状 | 对齐 |
|----|--------|------|------|
| P-BD-01 | 列容器 | `surface-solid` + `border` + 列头 `border-bottom` | ✅ A-02 |
| P-BD-02 | 卡片 | 列内 `--lanpm-bg` 卡片底 | ✅ 与列背景分层，保持 |
| P-BD-03 | 工具条 | `ViewToolbar` | 手验 A-06、A-07 |
| P-BD-04 | 空列占位 | dashed `border` | 手验亮色虚线可见性 |

### 3.4 任务树 Tree

| ID | 检查项 | 现状 | 对齐 |
|----|--------|------|------|
| P-TR-01 | 树面板 `.treeWrap` | `surface-solid` + `border` | ✅ A-02 |
| P-TR-02 | 详情 `.detailPanel` | 同上 | ✅ A-02 |
| P-TR-03 | 双栏间隙 | `gap: 12px` | ✅ A-05 |
| P-TR-04 | 窄屏堆叠 | ≤960px 详情 `max-height` | 手验分界仍可读 |

### 3.5 甘特 Gantt

| ID | 检查项 | 现状 | 对齐 |
|----|--------|------|------|
| P-GT-01 | 图表 `.chartWrap` | `surface-solid` + `border`；SVG 令牌覆盖 | ✅ A-02（VIS-FIX-01） |
| P-GT-02 | 禁用导出钮 | 暗色可读样式 | ✅ 已写 |
| P-GT-03 | `readCssVar` fallback | 亮色默认 hex | 低优；变量正常时无影响 |
| P-GT-04 | 工具条 `ViewSegment` | 日/周/月 | 手验 A-07、亮暗条颜色 |

### 3.6 文件 Files

| ID | 检查项 | 现状 | 对齐 |
|----|--------|------|------|
| P-FL-01 | 列表/预览分栏 | `listPane` / `previewPane`：`surface-solid` + `border` | ✅ A-02 |
| P-FL-02 | 预览区内文 | 部分 `--lanpm-bg` 嵌套 | 手验：预览区内是否需与外壳再分层 |
| P-FL-03 | 传输列表 `.transferList` | 有 border，底未显式指定 | 手验是否融入 `bg` |
| P-FL-04 | 窄屏单栏 | ≤960px | 手验列表/预览上下分界 |

### 3.7 驾驶舱 Cockpit

| ID | 检查项 | 现状 | 对齐 |
|----|--------|------|------|
| P-CK-01 | 指标区 | Ant `Card` / `Statistic`（ThemeProvider 底） | 手验亮色：Card 与 `--lanpm-bg` 对比是否够 |
| P-CK-02 | 项目行 | `border-bottom` only | 手验列表行间分界 |
| P-CK-03 | AI 报告 | `--lanpm-code-bg` 的 `pre` | ✅ 手验暗色可读 |
| P-CK-04 | 标题 | `ViewHeader` | ✅ A-06 |

### 3.8 群组壳层 GroupView + ViewHeader

| ID | 检查项 | 现状 | 对齐 |
|----|--------|------|------|
| P-GV-01 | 看板/树/甘特/文件 | `ViewHeader` + `margin-bottom: 8px`，**无**底部分隔线 | 四视图一致；若加强分界应**四页同改** |
| P-GV-02 | 聊天 | 无 `ViewHeader` 占位的双标题（标题在侧栏/上下文） | ✅ docs/04 §6.1 |
| P-GV-03 | `taskBody` | flex 填满 | 手验标题区与首行工具条间距 |

### 3.9 弹层 Modals

| ID | 模块 | 对齐 |
|----|------|------|
| P-MD-01 | 发现群组 `discover.module.css` | 列表行 hover `accent-fill`；手验 Modal 边与内容区 |
| P-MD-02 | 个人设置 / 数据与存储 | Ant Form + Panel；手验亮暗表单项边界 |
| P-MD-03 | 建群 / 任务编辑等 | 依赖 Ant token；抽样手验 |

### 3.10 已修复（勿重复）

| 项 | 说明 |
|----|------|
| `--lanpm-text-primary` | 已改 `--lanpm-text`；`verify:visual` 扫描未定义令牌 |

---

## 4. 待办（TodoList）

> 勾选仅维护本文件；全完成后更新 [CHANGELOG.md](./CHANGELOG.md)，并跑 `npm run verify:visual`。

### P0 — 聊天 + 全局（影响面最大）

- [ ] **DM-01** 聊天侧栏 `surface-solid` — `chat.module.css` `.sidebar`
- [ ] **DM-02** 聊天输入区 `surface-solid` — `.composer`
- [ ] **DM-03** 聊天亮/暗手验（侧栏 / 模式条 / 上下文 / 消息 / 输入五层）
- [ ] **DM-04** TopBar 工具区分组 — `TopBar.tsx` / `TopBar.module.css`
- [ ] **DM-05** 评估亮色 `--lanpm-border` / `--lanpm-bar-border` — `global.module.css`（成对 dark）
- [ ] **DM-06** 顶栏 + 底栏亮/暗手验

### P1 — 七页逐页手验（对齐 §2 标准 A-01～A-10）

- [ ] **DM-07** Setup — P-SU-01～04
- [ ] **DM-08** 看板 — P-BD-01～04
- [ ] **DM-09** 任务树 — P-TR-01～04
- [ ] **DM-10** 甘特 — P-GT-01～04
- [ ] **DM-11** 文件 — P-FL-01～04
- [ ] **DM-12** 驾驶舱 — P-CK-01～04
- [ ] **DM-13** 群组壳层 — P-GV-01～03（若加 ViewHeader 底部分隔，四任务视图同步）

### P2 — 弹层、文档、自动化

- [ ] **DM-14** 弹层抽样手验 — P-MD-01～03
- [ ] **DM-15** **亮/暗 × 七页** 对照矩阵打勾（见 §5）
- [ ] **DM-16** 更新 `docs/04` §6.1 / §1.5：聊天面色差 + 全页手验说明
- [ ] **DM-17** `npm run verify:visual` + `verify:visual-screenshots`（环境可用时）

### 明确不做

- 消息列表行间横线；底栏 Tab 加粗线；硬编码描边色；微信三栏/微信绿

---

## 5. 亮/暗 × 七页手验矩阵（DM-15）

完成时在格内打 `✓` 或记缺陷编号（如 P-CH-01）。

| 页面 | 亮色 | 暗色 | 备注 |
|------|------|------|------|
| Setup | [ ] | [ ] | |
| Chat | [ ] | [ ] | P0 改代码后重点 |
| Board | [ ] | [ ] | |
| Tree | [ ] | [ ] | |
| Gantt | [ ] | [ ] | |
| Files | [ ] | [ ] | |
| Cockpit | [ ] | [ ] | |
| TopBar+BottomNav | [ ] | [ ] | 各页切换时顺带 |

---

## 6. 实现索引

| 主题 | 文件 |
|------|------|
| 令牌 | `src/renderer/src/styles/global.module.css` |
| Ant 主题 | `src/renderer/src/app/ThemeProvider.tsx` |
| 主壳 | `layout/MainLayout.module.css` |
| 七页 | `features/chat/chat.module.css`、`board/board.module.css`、`tree/tree.module.css`、`gantt/gantt.module.css`、`files/files.module.css`、`setup/SetupWizard.module.css`、`views/CockpitView.module.css` |
| 群组壳 | `views/GroupView.tsx`、`ui/ViewHeader.module.css` |
| 门禁 | `tests/static/verify-visual.ts` |

---

## 7. 完成定义

1. **聊天页**亮色：侧栏、输入与消息区一眼可分；暗色无过粗线或断层。  
2. **七页 + 顶底栏**在 §5 矩阵亮/暗全部手验勾选，无未记录的 P0 视觉缺陷。  
3. 看板/树/甘特/文件/驾驶舱/Setup 满足 §2 **A-01～A-10**（允许「已符合仅手验」）。  
4. `npm run verify:visual` 通过。  
5. 本文件 Todo 全 `[x]` 后摘要写入 `CHANGELOG.md`。

---

## 8. 参考

- [plan.md](./plan.md) · [docs/06 §2.6](./docs/06_验收与里程碑计划.md#26-手验待办ssot)  
- `archive/audit/visual/20260529_165655_视觉一致性校验_rc35.md`
